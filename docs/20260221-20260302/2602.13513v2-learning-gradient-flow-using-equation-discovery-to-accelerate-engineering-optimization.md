---
title: "Learning Gradient Flow: Using Equation Discovery to Accelerate Engineering Optimization"
title_zh: 学习梯度流：利用方程发现加速工程优化
authors: "Grant Norman, Conor Rowan, Kurt Maute, Alireza Doostan"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13513v2"
tags: ["query:sr"]
score: 9.0
evidence: 动力系统和优化的数据驱动方程发现
tldr: 本研究针对工程优化中目标函数及其梯度计算昂贵的问题，提出了一种基于数据驱动方程发现的“学习梯度流”（LGF）优化器。该方法通过分析梯度下降、牛顿法或ADAM的优化轨迹数据，学习其连续时间动力学并构建代理模型。LGF支持全维或降维空间的变量多项式建模，能有效预测优化路径。在逆问题、拓扑优化及科学机器学习等任务中的实验表明，该方法在减少昂贵函数评估的同时，显著提升了收敛效率。
selection_source: fresh_fetch
motivation: 旨在降低复杂工程优化问题中频繁评估目标函数及其梯度所带来的巨大计算成本。
method: 引入LGF优化器，利用方程发现技术从优化轨迹中提取动力学特征，构建连续时间梯度流的代理模型。
result: 在结构拓扑优化、逆问题和前向求解等多个标准工程案例中，LGF显著加快了优化过程的收敛速度。
conclusion: 学习梯度流方法能有效捕捉优化轨迹的关键特征，是加速高成本工程优化问题的有力工具。
---

## 摘要
在这项工作中，我们研究了利用动力系统的数据驱动方程发现技术来建模和预测无约束优化问题的连续时间动力学。为了避免对目标函数及其梯度进行昂贵的评估，我们利用优化变量的轨迹数据来学习与梯度下降、牛顿法和 ADAM 优化相关的连续时间动力学。随后，将发现的梯度流作为原始优化问题的代理模型进行求解。为此，我们引入了学习梯度流 (LGF) 优化器，该优化器能够在优化过程中以用户定义的时间间隔，在全维或降维空间中构建变多项式阶数的代理模型。我们在工程力学和科学机器学习的几个标准问题上证明了该方法的有效性，包括两个反问题、结构拓扑优化以及两个具有不同离散化的正向求解问题。我们的结果表明，学习到的梯度流可以通过捕捉优化轨迹的关键特征，同时避免对目标及其梯度进行昂贵的评估，从而显著加快收敛速度。

## Abstract
In this work, we investigate the use of data-driven equation discovery for dynamical systems to model and forecast continuous-time dynamics of unconstrained optimization problems. To avoid expensive evaluations of the objective function and its gradient, we leverage trajectory data on the optimization variables to learn the continuous-time dynamics associated with gradient descent, Newton's method, and ADAM optimization. The discovered gradient flows are then solved as a surrogate for the original optimization problem. To this end, we introduce the Learned Gradient Flow (LGF) optimizer, which is equipped to build surrogate models of variable polynomial order in full- or reduced-dimensional spaces at user-defined intervals in the optimization process. We demonstrate the efficacy of this approach on several standard problems from engineering mechanics and scientific machine learning, including two inverse problems, structural topology optimization, and two forward solves with different discretizations. Our results suggest that the learned gradient flows can significantly expedite convergence by capturing critical features of the optimization trajectory while avoiding expensive evaluations of the objective and its gradient.

---

## 论文详细总结（自动生成）

### 论文总结：学习梯度流（Learned Gradient Flow, LGF）

#### 1. 核心问题与整体含义（研究动机和背景）
在工程设计、逆问题求解和科学机器学习中，优化过程通常极其昂贵。这是因为每次迭代都需要评估目标函数及其梯度，而这往往涉及求解大规模偏微分方程（PDE）。
**研究动机**：利用“优化算法在连续时间极限下可视为动力系统（梯度流）”这一物理特性，通过数据驱动的**方程发现（Equation Discovery）**技术，学习优化变量随“时间”（迭代次数）演化的规律。
**核心意义**：构建一个廉价的常微分方程（ODE）代理模型来预测未来的优化轨迹，从而跳过大量昂贵的真实梯度计算，显著加速收敛。

#### 2. 方法论
论文提出了 **Learned Gradient Flow (LGF)** 框架，其核心步骤如下：
*   **数据生成阶段**：运行 $K$ 步原始优化算法（如梯度下降、牛顿法或 ADAM），记录优化变量的轨迹数据。
*   **方程发现（SINDy）**：使用稀疏非线性动力学识别（SINDy）方法，将优化变量的变化率建模为状态变量的多项式组合。
*   **代理演化阶段**：求解学习到的 ODE，将优化变量从当前状态直接“外推”到第 $M$ 步（$M > K$），此过程无需评估目标函数。
*   **高维处理（降维）**：针对高维问题（如拓扑优化），先通过奇异值分解（SVD）将数据投影到低维潜空间，在潜空间学习动力学后再映射回原空间。
*   **多算法适配**：
    *   **牛顿法**：学习包含二阶信息的连续时间牛顿流。
    *   **ADAM**：由于 ADAM 具有动量和历史依赖性，LGF 通过学习梯度的代理模型并将其嵌入到 ADAM 的连续时间 ODE 系统中来实现加速。

#### 3. 实验设计
论文在五个具有代表性的工程和机器学习场景中验证了 LGF：
1.  **1D 动态逆问题**：恢复加热棒的材料导热系数（对比标准梯度下降）。
2.  **3D 结构拓扑优化**：高维密度场优化（13,824 个变量，对比梯度下降 + 降维技术）。
3.  **非线性热传导前向求解**：基于谱方法的能量泛函极小化（对比牛顿法）。
4.  **全波反演（FWI）**：地球物理中的非凸逆问题（对比 ADAM）。
5.  **Deep Ritz 方法**：使用神经网络求解 3D 弹性力学问题，包含全量和随机小批量（Mini-batch）梯度场景（对比 ADAM）。
**Benchmark**：各场景对应的标准优化算法（GD, Newton, ADAM）。

#### 4. 资源与算力
*   **软件环境**：使用 PyTorch 框架实现，ODE 求解使用了 `torchdiffeq` 库。
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。考虑到涉及 3D 有限元和神经网络训练，推测使用了常规科研级工作站。
*   **计算开销**：论文强调了 LGF 的 SVD 和 SINDy 求解成本远低于 PDE 的前向/伴随求解成本。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 5 组核心实验，涵盖了线性/非线性物理、谱方法/有限元离散、前向/逆问题等维度。
*   **充分性**：实验设计较为全面，不仅展示了成功案例，还通过消融实验（如改变加速比 $M/K$）展示了当预测过于激进时模型失效的情况。
*   **客观性**：对比实验均保持了相同的学习率和初始化条件，结果具有说服力。但在超参数（如 $K$ 和 $M$ 的选择）的自动化搜索方面略显不足。

#### 6. 主要结论与发现
*   **显著加速**：LGF 在保持与原始算法相似精度的前提下，实现了 **33% 到 250%** 的计算加速（即减少了相应比例的梯度评估次数）。
*   **低维特性**：即使是上万维的拓扑优化问题，其优化轨迹往往也遵循极低维（如 $r=2$）的动力学规律。
*   **鲁棒性**：该方法能有效处理包含噪声梯度的随机优化问题（如 Deep Ritz 的小批量训练）。

#### 7. 优点
*   **创新性强**：首次将 SINDy 方程发现与主流工程优化算法（尤其是 ADAM 和牛顿法）深度结合。
*   **非侵入性**：不需要修改底层的 PDE 求解器，只需记录优化轨迹即可构建代理模型。
*   **灵活性**：支持降维处理，使其能够扩展到超大规模的工程设计问题。

#### 8. 不足与局限
*   **超参数敏感性**：历史窗口大小 $K$ 和预测步数 $M$ 需要手动调优，缺乏自动化的启发式准则。
*   **约束处理**：目前仅限于无约束优化，对于包含复杂等式/不等式约束的问题，轨迹可能会在约束边界发生突变，导致 ODE 学习失效。
*   **外推风险**：如果优化轨迹进入了之前未观察到的新物理区域（如非凸景观中的剧烈转向），代理模型可能会产生较大偏差。

（完）
