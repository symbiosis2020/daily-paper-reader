---
title: Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression
title_zh: 突破摊销神经符号回归中的简化瓶颈
authors: "Paul Saegert, Ullrich Köthe"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08885v4"
tags: ["query:sr"]
score: 10.0
evidence: 摊销神经符号回归与化简
tldr: 符号回归旨在发现可解释的数学表达式，但摊销神经符号回归常受限于表达式简化的计算瓶颈。本文提出 SimpliPy 简化引擎，其速度比 SymPy 快 100 倍，并基于此构建了 Flash-ANSR 框架。该方法通过高效的规则化简化，实现了大规模训练集的扩展和系统性去重，在保持高推理效率的同时，显著提升了复杂表达式的恢复精度和简洁度。
selection_source: fresh_fetch
motivation: 现有的摊销符号回归方法因依赖计算缓慢的通用代数系统进行表达式简化，严重限制了模型的训练规模、推理速度及对复杂公式的处理能力。
method: 开发了高性能规则化简化引擎 SimpliPy 并集成至 Flash-ANSR 框架，通过大幅提升简化效率来支持更大规模的训练集和更有效的表达式去重。
result: SimpliPy 在保持简化质量的同时实现了百倍加速，使 Flash-ANSR 在基准测试中显著优于同类摊销方法，并达到了与最先进的直接优化方法 PySR 相当的精度。
conclusion: 通过解决表达式简化的计算瓶颈，摊销符号回归可以在保证极高推理效率的同时，实现与传统优化方法相媲美的性能和更优的表达式简洁性。
---

## 摘要
符号回归 (SR) 旨在发现能够准确描述观测数据的可解释解析表达式。摊销 SR 有望比主流的遗传编程 SR 方法更高效，但目前在扩展到现实科学复杂度方面仍面临挑战。我们发现，一个关键障碍是缺乏将等价表达式快速约简为简洁规范形式的方法。摊销 SR 曾通过 SymPy 等通用计算机代数系统 (CAS) 来解决这一问题，但高昂的计算成本严重限制了训练和推理速度。我们提出了 SimpliPy，这是一种基于规则的简化引擎，在质量相当的情况下，其速度比 SymPy 快 100 倍。这为摊销 SR 带来了实质性的改进，包括对更大训练集的扩展性、对每个表达式 token 预算更高效的利用，以及针对等价测试表达式的系统性训练集去污染。我们在 Flash-ANSR 框架中展示了这些优势，该框架在 FastSRB 基准测试上的准确率远高于摊销基准模型（NeSymReS、E2E）。此外，它的性能与最先进的直接优化方法 (PySR) 相当，且随着推理预算的增加，它能恢复出更简洁而非更复杂的表达式。

## 速览
**TLDR**：符号回归旨在发现可解释的数学表达式，但摊销神经符号回归常受限于表达式简化的计算瓶颈。本文提出 SimpliPy 简化引擎，其速度比 SymPy 快 100 倍，并基于此构建了 Flash-ANSR 框架。该方法通过高效的规则化简化，实现了大规模训练集的扩展和系统性的数据去重，在保持高推理效率的同时，显著提升了复杂表达式的恢复精度和简洁度。 \
**Motivation**：现有的摊销符号回归方法因依赖计算缓慢的通用代数系统进行表达式简化，导致训练和推理效率低下，难以处理复杂的科学问题。 \
**Method**：开发了高性能规则化简化引擎 SimpliPy，并将其集成到 Flash-ANSR 框架中，以支持大规模数据集的快速训练和等价表达式的去重。 \
**Result**：实验表明 SimpliPy 实现了百倍加速，使 Flash-ANSR 在精度上显著超越现有摊销基准，并达到了与最先进的直接优化方法 PySR 相当的水平。 \
**Conclusion**：突破表达式简化的性能瓶颈是提升摊销符号回归扩展性的关键，该研究证明了高效简化引擎能显著增强模型处理复杂科学问题的能力。

---

## Abstract
Symbolic regression (SR) aims to discover interpretable analytical expressions that accurately describe observed data. Amortized SR promises to be much more efficient than the predominant genetic programming SR methods, but currently struggles to scale to realistic scientific complexity. We find that a key obstacle is the lack of a fast reduction of equivalent expressions to a concise normalized form. Amortized SR has addressed this by general-purpose Computer Algebra Systems (CAS) like SymPy, but the high computational cost severely limits training and inference speed. We propose SimpliPy, a rule-based simplification engine achieving a 100-fold speed-up over SymPy at comparable quality. This enables substantial improvements in amortized SR, including scalability to much larger training sets, more efficient use of the per-expression token budget, and systematic training set decontamination with respect to equivalent test expressions. We demonstrate these advantages in our Flash-ANSR framework, which achieves much better accuracy than amortized baselines (NeSymReS, E2E) on the FastSRB benchmark. Moreover, it performs on par with state-of-the-art direct optimization (PySR) while recovering more concise instead of more complex expressions with increasing inference budget.

---

## 论文详细总结（自动生成）

这是一份关于论文《Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：摊销神经符号回归（Amortized SR）虽然推理速度快，但在训练和推理中面临**“简化瓶颈”**。
*   **研究背景**：
    *   符号回归旨在从观测数据中发现简洁的数学公式。
    *   现有的摊销方法要么依赖通用的计算机代数系统（如 SymPy）进行公式简化，导致数据生成速度比模型更新慢几个数量级；要么完全放弃简化，导致训练数据充斥着冗余表达（如 $x+x$ 与 $2x$），降低了学习效率和公式的可解释性。
*   **研究动机**：开发一种极速的简化引擎，以支持大规模、高质量的符号回归模型训练，从而弥合摊销方法与传统遗传编程（GP）方法之间的性能差距。

### 2. 方法论
论文提出了 **Flash-ANSR** 框架，其核心技术包括：
*   **SimpliPy 简化引擎**：
    *   **离线规则发现**：采用 Kruskal 算法变体，在预定义的表达式空间内构建“最小生成森林”，提取最精简的等价转换规则。
    *   **在线模式匹配**：将代数简化简化为高效的哈希查找和模式替换，避免了复杂的树遍历。
    *   **性能**：在保持与 SymPy 相当的简化质量下，实现了 **100 倍以上** 的加速。
*   **Flash-ANSR 架构**：
    *   **编码器**：改进的 **Set Transformer**，引入了 Masked RMSSetNorm（掩码均方根归一化），能够高效处理变长的输入数据集。
    *   **解码器**：基于 **Transformer Decoder**，使用 FlashAttention 和旋转位置嵌入（RoPE），按前缀表达式顺序生成公式 Token。
*   **训练流程**：
    *   **实时数据生成**：利用 SimpliPy 的高速特性，在训练循环中实时生成并简化表达式。
    *   **严格去污染**：通过符号和数值双重校验，确保训练集中不包含任何与测试集等价的表达式，保证了实验的严谨性。

### 3. 实验设计
*   **数据集/基准测试**：主要使用 **FastSRB** 基准测试，该测试集基于物理规律（SRSD），比传统的 Feynman 或 Nguyen 数据集更符合真实的科学应用场景。
*   **对比方法**：
    *   **摊销类基准**：NeSymReS、E2E。
    *   **直接优化类（状态最先进）**：PySR（基于遗传编程）。
*   **评估维度**：推理时间与恢复率的 Pareto 前沿、数据稀疏性影响、噪声鲁棒性、公式简洁度。

### 4. 资源与算力
论文详细列出了四个规模模型的训练配置：
*   **v23.0-3M**：RTX 2080Ti，训练 112 小时。
*   **v23.0-20M**：RTX 4090，训练 56 小时。
*   **v23.0-120M**：A100，训练 340 小时。
*   **v23.0-1B**：H200，训练 657 小时。
*   **推理环境**：单台高端工作站（AMD Ryzen 9 9950X + RTX 4090）。

### 5. 实验数量与充分性
*   **实验规模**：训练过程使用了高达 **5.12 亿** 个实时生成的表达式对。
*   **实验覆盖**：
    *   针对 115 个物理公式进行了详尽的推理时间缩放实验。
    *   进行了数据点数量（从 1 到 2048）的稀疏性实验。
    *   进行了不同噪声水平（$10^{-3}$ 到 $10^{-1}$）的鲁棒性实验。
    *   包含对 SimpliPy 效率、训练先验分布（常数范围）的消融实验。
*   **评价**：实验设计非常充分且客观。特别是引入了严格的**数据去污染**和**推理时间标准化对比**，解决了符号回归领域长期存在的评估标准不统一、数据泄露风险等问题。

### 6. 主要结论与发现
*   **打破瓶颈**：SimpliPy 成功消除了简化过程的计算负担，使得大规模在线训练成为可能。
*   **性能飞跃**：Flash-ANSR 在 FastSRB 上的表现远超之前的摊销方法。120M 和 1B 模型在推理预算充足时，性能达到了与 PySR 相当的水平。
*   **简洁性反转（Parsimony Inversion）**：这是一个重要发现。随着推理计算量的增加，PySR 倾向于生成更复杂的公式来拟合残差，而 Flash-ANSR 则倾向于从后验分布中采样出更简洁、更接近真值的“正确”公式。
*   **双下降现象**：在数据稀疏性实验中观察到了类似于深度学习的“双下降”现象，识别出了插值区与识别区的转换边界。

### 7. 亮点
*   **工程创新**：SimpliPy 引擎解决了符号回归在大规模预训练中的核心痛点。
*   **评估严谨性**：建立了极其严格的测试协议（数值恢复精度要求达到 $10^{-7}$），并开源了去污染工具。
*   **扩展性**：证明了符号回归模型也符合 Scaling Laws，通过增加参数量和训练数据可以显著提升泛化能力。

### 8. 不足与局限
*   **噪声鲁棒性**：在面对高水平噪声（$\eta \ge 10^{-2}$）时，Flash-ANSR 的表现不如 PySR。这是因为模型将噪声误认为是复杂的函数结构（分布偏移）。
*   **训练成本**：1B 参数模型的训练需要昂贵的 H200 算力支持，对于普通研究者门槛较高。
*   **算力依赖**：虽然推理很快，但要达到最佳精度仍需要大量的采样（如 64k 次采样），这在一定程度上抵消了摊销方法的效率优势。

（完）
