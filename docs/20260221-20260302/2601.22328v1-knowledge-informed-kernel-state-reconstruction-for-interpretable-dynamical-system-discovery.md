---
title: Knowledge-Informed Kernel State Reconstruction for Interpretable Dynamical System Discovery
title_zh: 知识告知的核状态重构：用于可解释动力系统发现
authors: "Luca Muscarnera, Silas Ruhrberg Estévez, Samuel Holt, Evgeny Saveliev, Mihaela van der Schaar"
date: 2026-01-29
pdf: "https://arxiv.org/pdf/2601.22328v1"
tags: ["query:sr"]
score: 9.0
evidence: 将守恒定律等结构和语义先验纳入符号发现过程
tldr: 从噪声和部分观测数据中恢复控制方程是科学发现的核心挑战。本文提出MAAT框架，通过知识告知的核状态重构（KSR）将物理先验（如守恒律、非负性）直接融入再生核希尔伯特空间的状态估计中。该方法能从异构采样数据中生成物理一致的平滑轨迹及其解析导数，为符号回归提供了可靠接口，在多个科学基准测试中显著提升了动力系统发现的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的动力系统发现方法在面对高噪声、部分观测或异构采样数据时表现不佳，且往往依赖缺乏物理可解释性的黑盒模型。
method: 提出MAAT框架，利用核状态重构技术并在优化目标中直接嵌入结构化物理先验和领域特定的观测模型。
result: 在12个科学基准测试和多种噪声环境下，MAAT在状态估计均方误差和导数计算精度上均显著优于强基线方法。
conclusion: 通过将物理知识融入核重构过程，MAAT为从碎片化传感器数据中进行可解释的符号发现提供了一个原则性的接口。
---

## 摘要
从数据中恢复控制方程是科学发现的核心，然而现有方法在噪声干扰、部分观测的情况下往往失效，或者依赖于掩盖机制的黑盒潜动力学。我们引入了 MAAT（模型感知轨迹近似），这是一个建立在知识告知的核状态重构基础上的符号发现框架。MAAT 在再生核希尔伯特空间中制定状态重构，并将结构和语义先验（如非负性、守恒律和特定领域的观测模型）直接纳入重构目标，同时适应异构采样和测量粒度。这产生了具有解析时间导数的平滑且物理一致的状态估计，为碎片化的传感器数据与符号回归之间提供了一个原则性的接口。在十二个不同的科学基准测试和多种噪声环境下，与强基线模型相比，MAAT 显著降低了下游符号回归所使用的轨迹及导数的状态估计均方误差（MSE）。

## Abstract
Recovering governing equations from data is central to scientific discovery, yet existing methods often break down under noisy, partial observations, or rely on black-box latent dynamics that obscure mechanism. We introduce MAAT (Model Aware Approximation of Trajectories), a framework for symbolic discovery built on knowledge-informed Kernel State Reconstruction. MAAT formulates state reconstruction in a reproducing kernel Hilbert space and directly incorporates structural and semantic priors such as non-negativity, conservation laws, and domain-specific observation models into the reconstruction objective, while accommodating heterogeneous sampling and measurement granularity. This yields smooth, physically consistent state estimates with analytic time derivatives, providing a principled interface between fragmented sensor data and symbolic regression. Across twelve diverse scientific benchmarks and multiple noise regimes, MAAT substantially reduces state-estimation MSE for trajectories and derivatives used by downstream symbolic regression relative to strong baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Knowledge-Informed Kernel State Reconstruction for Interpretable Dynamical System Discovery》的结构化深度总结：

### 1. 核心问题与整体含义
论文针对科学发现中的一个核心瓶颈：**如何从噪声大、采样频率不一且仅能部分观测的数据中，准确恢复动力系统的控制方程（如微分方程）**。
现有的符号回归（SR）方法（如 SINDy）高度依赖高质量的导数计算，但在现实场景（如临床医疗、药理学）中，数据往往是碎片化的。现有的平滑技术（如样条曲线）缺乏物理约束，而黑盒模型（如 Neural ODEs）则缺乏可解释性。MAAT 旨在通过将物理先验融入核重构过程，在“数据灵活性”与“物理一致性”之间建立桥梁。

### 2. 方法论：MAAT 框架
MAAT 的核心思想是将状态重构定义为**再生核希尔伯特空间（RKHS）中的正则化风险最小化问题**。

*   **核状态重构 (KSR)**：将潜轨迹建模为核函数的线性组合。利用高斯核的无限可微性，可以直接获得解析形式的时间导数，避免了数值微分带来的噪声放大。
*   **知识告知的优化目标**：其损失函数包含四个部分：
    1.  **数据拟合项**：处理稀疏的直接观测和密集的聚合观测（通过观测算子 $H$）。
    2.  **平滑正则项**：RKHS 范数约束。
    3.  **导数惩罚项**：根据先验知识约束导数幅度，防止轨迹剧烈波动。
    4.  **物理先验约束**：通过惩罚项强制执行非负性（如浓度不能为负）、质量守恒或单调性。
*   **两阶段流程**：首先通过 KSR 得到平滑且物理一致的轨迹及其解析导数；随后将这些高质量数据输入符号回归后端（如 PySR 或 SINDy）以发现显式方程。

### 3. 实验设计
*   **数据集/场景**：涵盖了 12 个跨学科基准测试，包括：
    *   **药理学/生物学**：2型糖尿病模拟器（16维）、结直肠癌模型、中性粒细胞生命周期、促红细胞生成素（EPO）动力学。
    *   **流行病学**：SEIR、SEIRH 传染病模型、病毒动力学。
    *   **经济与生态**：消费者社会模型、Lotka-Volterra 捕食者模型。
*   **噪声环境**：测试了各向同性高斯噪声、相关高斯噪声（AR(1)）和重尾 Student-t 噪声。
*   **对比方法**：
    *   **数值平滑**：三次样条、Savitzky-Golay 滤波、TVRegDiff、线性插值、RBF 插值。
    *   **统计/深度学习**：高斯过程（GP）、卡尔曼滤波（RTS 平滑）、Neural ODE、PINN（物理信息神经网络）、Symbolic Neural ODE。

### 4. 资源与算力
*   **硬件**：使用了 AMD Ryzen Threadripper PRO 3995WX (64核) CPU 和单张 **NVIDIA RTX A4000 (16GB)** GPU。
*   **训练时长**：MAAT 的平均运行时间约为 **44.7 秒**，介于传统平滑方法（约20秒）和深度学习方法（Neural ODE 约630秒）之间。
*   **实现**：基于 JAX 框架，利用 JIT 编译加速。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个数据集上分别进行了 3 种噪声类型的测试，每组实验重复 10 个随机种子并计算 95% 置信区间。
*   **消融与验证**：进行了关于非负性约束、导数惩罚以及结构化先验（如守恒律）的消融实验。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了重构误差（MSE），还评估了下游符号回归的成功率，并涵盖了从低维到高维（16维）的复杂系统，证明了方法的通用性。

### 6. 主要结论与发现
*   **精度提升**：在所有噪声环境下，MAAT 的状态估计 MSE 显著低于基线，尤其在处理异构观测（部分变量密集、部分变量稀疏）时表现卓越。
*   **导数鲁棒性**：相比有限差分，MAAT 的解析导数避免了高频噪声放大，为符号回归提供了更稳定的输入。
*   **先验的力量**：引入非负性和守恒律等简单物理约束，能显著提升在极稀疏数据下的重构质量。
*   **符号发现成功率**：使用 MAAT 处理后的数据，下游 PySR 和 SINDy 恢复正确方程的准确率大幅提高。

### 7. 优点与亮点
*   **物理与数据的融合**：通过正则化项优雅地将“硬”物理限制转化为“软”约束，解决了纯数据驱动方法不符合物理常识的问题。
*   **处理异构数据**：能够同时融合不同频率、不同观测算子的传感器数据，非常贴合临床实际。
*   **解析导数**：利用核方法的特性直接求导，比数值微分更鲁棒，比 Neural ODE 更高效。
*   **凸优化特性**：相比 Neural ODE 的非凸优化，MAAT 的重构过程更易收敛且对超参数不敏感。

### 8. 不足与局限
*   **计算复杂度**：尽管使用了优化算法，但核方法本质上仍面临 $O(N^2)$ 或 $O(N^3)$ 的扩展性挑战，在极长序列上可能受限。
*   **先验依赖**：如果注入的物理先验错误（例如错误地假设了守恒律），可能会导致重构结果偏离真实轨迹。
*   **结构已知性**：目前假设状态变量的分层分区（Subsystem partition）是预先已知的，但在某些复杂系统中，如何自动划分这些模块仍是挑战。

（完）
