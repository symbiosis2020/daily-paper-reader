---
title: Optimal Control-Based Falsification of Learnt Dynamics via Neural ODEs and Symbolic Regression
title_zh: 基于最优控制的、通过神经常微分方程和符号回归对学习动力学进行证伪
authors: "Lasse Kötz, Jonas Sjöberg, Knut Åkesson"
date: 2026-01-18
pdf: "https://arxiv.org/pdf/2602.00031v1"
tags: ["query:sr"]
score: 9.0
evidence: 通过符号回归将神经ODE转换为解析形式以提高可解释性
tldr: 本研究提出一种基于最优控制的伪证框架，用于验证信号时序逻辑（STL）规范下的网络实体系统。该方法利用神经常微分方程（Neural ODEs）学习未知动力学，并通过符号回归将其转化为解析形式，从而实现高效的轨迹优化。通过最小化STL鲁棒性生成候选反例，并在原系统中验证，利用伪反例迭代优化模型。该方法在ARCH-COMP 2024基准测试中表现优异，显著减少了实验次数。
selection_source: fresh_fetch
motivation: 传统的基于优化的伪证方法在不建模系统动力学的情况下，往往需要大量的系统实验，导致验证效率低下。
method: 结合神经ODE学习动力学与符号回归获取解析模型，将伪证问题转化为基于STL鲁棒性最小化的最优控制问题。
result: 在ARCH-COMP 2024基准测试中，该方法所需的系统实验次数比不建模动力学的优化方法减少了数个数量级。
conclusion: 将学习到的代理模型与最优控制相结合，能有效提升CPS伪证过程的效率、可解释性及数据利用率。
---

## 摘要
我们提出了一种证伪框架，该框架将学习到的代理动力学与最优控制相结合，以高效地为信号时序逻辑（STL）规范下的信息物理系统（CPS）生成反例。未知系统动力学通过神经常微分方程（Neural ODEs）进行识别，同时将已知的先验结构直接嵌入模型中，从而降低了数据需求。学习到的神经常微分方程通过符号回归转换为解析形式，从而实现快速且可解释的轨迹优化。证伪过程被转化为在输入轨迹上最小化 STL 鲁棒性的问题；负鲁棒性产生候选反例，并在原始系统上进行验证。虚假轨迹被迭代地用于改进代理模型，而真实反例则作为最终结果返回。在 ARCH-COMP 2024 基准测试上的实验表明，与不建模系统动力学的基于优化的方法相比，该方法对受测系统进行的实验次数减少了几个数量级。

## Abstract
We present a falsification framework that integrates learned surrogate dynamics with optimal control to efficiently generate counterexamples for cyber-physical systems specified in signal temporal logic (STL). The unknown system dynamics are identified using neural ODEs, while known a-priori structure is embedded directly into the model, reducing data requirements. The learned neural ODE is converted into an analytical form via symbolic regression, enabling fast and interpretable trajectory optimization. Falsification is cast as minimizing STL robustness over input trajectories; negative robustness yields candidate counterexamples, which are validated on the original system. Spurious traces are iteratively used to refine the surrogate, while true counterexamples are returned as final results. Experiments on ARCH-COMP 2024 benchmarks show that this method requires orders of magnitude fewer experiments of the system under test than optimization-based approaches that do not model system dynamics.

---

## 论文详细总结（自动生成）

以下是对论文《Optimal Control-Based Falsification of Learnt Dynamics via Neural ODEs and Symbolic Regression》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着网络实体系统（CPS）复杂性增加，传统形式验证方法难以扩展。证伪（Falsification）作为一种替代手段，旨在寻找违反安全规范（通常以信号时序逻辑 STL 描述）的反例。
*   **核心问题**：现有的黑盒证伪方法（如随机搜索、贝叶斯优化）往往忽略了系统动力学信息，导致需要对真实系统（SUT）进行大量昂贵的实验或仿真。
*   **研究动机**：通过学习系统的动力学代理模型，并利用最优控制理论指导搜索，从而以极少的实验次数高效地发现系统漏洞。

### 2. 方法论
该论文提出了一种名为 **FalConN** 的框架，其核心流程如下：
*   **神经代理建模（Neural ODEs）**：使用神经常微分方程学习系统动力学。其优势在于可以嵌入已知的物理先验结构（$f_k$），仅让神经网络学习未知的动力学部分（$f_\theta$），提高数据效率。
*   **符号蒸馏（Symbolic Distillation）**：利用符号回归（SR）技术将训练好的神经 ODE 转化为解析数学表达式。这一步的关键意义在于为后续优化提供**精确的解析梯度**，避免数值梯度的噪声和计算开销。
*   **最优控制合成（OCP）**：将证伪问题转化为一个最优控制问题。目标是最小化 STL 鲁棒性值（负值代表违反规范）。
    *   使用 **Log-Sum-Exponential (LSE)** 平滑近似 STL 中的非光滑算子（min/max）。
    *   使用**直接配点法（Direct Collocation）**将连续动力学离散化。
    *   调用 **SNOPT** 优化器求解最易触发违规的输入轨迹。
*   **迭代验证**：在真实系统上验证候选反例。若为“虚假反例”（代理模型不准），则将该轨迹加入数据集重新训练，实现模型的迭代改进。

### 3. 实验设计
*   **实验场景**：采用 ARCH-COMP 2024 基准测试中的“神经网络控制磁悬浮系统”（Levitating Magnet）。
*   **测试规范**：包括三个 STL 规范（NN, NN $\beta=0.04$, NNx），涉及设定点跟踪、收敛速度和稳态误差。
*   **对比基准**：与 ARCH-COMP 2024 竞赛中的 8 种主流工具进行对比，包括 ARIsTEO、ATheNA、EXAM-Net、ForeSee、Moonlight、OD、$\Psi$-TaLiRo 等。

### 4. 资源与算力
*   **硬件环境**：13th Gen Intel Core i7-13700H CPU, 32GB RAM。
*   **软件实现**：基于 Julia 语言，使用了 `SymbolicRegression.jl` 进行符号回归，`Drake` 框架下的 `SNOPT` 求解器进行最优控制优化。
*   **算力说明**：文中未提及使用 GPU，主要计算压力在于 Neural ODE 的训练（ADAM + L-BFGS 算法）和符号回归的遗传算法搜索。

### 5. 实验数量与充分性
*   **实验规模**：针对每个规范重复进行了 **10 次独立实验**，以消除随机性影响。
*   **评价指标**：证伪成功率（FR）、平均实验次数（$\bar{S}$）和中位数实验次数（$\tilde{S}$）。
*   **充分性评价**：实验对比了多种不同原理的工具（基于搜索、基于贝叶斯优化、基于生成对抗网络等），覆盖了最新的学术前沿。虽然仅针对磁悬浮一个系统进行了深入测试，但该系统具有代表性的非线性特征。

### 6. 主要结论与发现
*   **极高的实验效率**：FalConN 在寻找反例时所需的真实系统实验次数比传统方法少 **1 到 2 个数量级**。例如，在某些规范下平均仅需 2.4 到 3 次实验，而其他工具可能需要数百次。
*   **模型精度**：Neural ODE 能够很好地捕捉非线性动力学，即使在数据极少的情况下也能通过外推引导优化器走向违规区域。
*   **符号化的价值**：符号回归生成的解析模型不仅提高了优化速度，还增强了代理模型的可解释性。

### 7. 优点与亮点
*   **混合建模**：支持将已知物理规律与神经网络结合，降低了纯数据驱动模型的训练难度。
*   **解析梯度**：通过符号蒸馏获取解析梯度，使得最优控制问题的求解比基于数值梯度的黑盒优化更稳定、更精确。
*   **端到端闭环**：建立了一个从数据采集、模型学习到自动化攻击（证伪）的完整闭环。

### 8. 不足与局限
*   **系统类型受限**：目前仅适用于连续动力学系统，尚未支持包含离散切换逻辑的**混合系统（Hybrid Systems）**。
*   **局部最优风险**：最优控制求解器（SNOPT）基于梯度，容易陷入局部最优，可能错过某些全局违规行为。
*   **符号回归开销**：符号回归的搜索空间随复杂度呈指数增长，对于极高维度的系统可能存在扩展性问题。
*   **基准覆盖面**：实验仅限于磁悬浮系统，未来需在更多样化（如自动驾驶、医疗器械）的基准上验证泛化能力。

（完）
