---
title: Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation
title_zh: 通过符号蒸馏获得的可解释解析卡拉比-丘度量
authors: D Yang Eng
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07834v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用符号回归将神经网络近似蒸馏为可解释公式
tldr: 本研究针对弦理论中难以计算的卡拉比-丘流形度规，提出了一种基于符号回归的蒸馏方法。通过将复杂的神经网络近似模型转化为简洁、可解释的数学公式，研究者仅用五个项就达到了与神经网络相当的精度（R²=0.9994），且参数量减少了3000倍。该公式不仅能跨模空间保持稳定，还能准确复现体积积分和汤川耦合等物理观测值，为复杂几何量的解析表达提供了新途径。
selection_source: fresh_fetch
motivation: 旨在解决卡拉比-丘流形度规计算困难且神经网络模型缺乏可解释性的问题。
method: 利用符号回归技术从预训练的神经网络教师模型中提取简洁的解析表达式。
result: 得到的五项公式在极低参数量下实现了极高的拟合精度，并成功预测了关键物理量。
conclusion: 符号蒸馏能够将黑盒模型转化为紧凑、可解释的解析模型，为物理和几何研究提供了有效的经验假设。
---

## 摘要
卡拉比-丘流形对弦理论至关重要，但其度量的计算往往难以处理。在本文中，我们展示了符号回归可以将神经网络近似蒸馏为简单且可解释的公式。我们的五项表达式在参数量减少 3000 倍的情况下，达到了与神经网络相当的精度（$R^2 = 0.9994$）。多种子验证确认了几何约束会筛选出基本特征（特别是幂和与对称多项式），同时允许结构多样性。在所研究的模参数范围（$ψ \in [0, 0.8]$）内，该函数形式可以保持不变，且系数平滑变化；我们将这些趋势解释为在局部训练的教师模型精度范围内（当 $ψ \neq 0$ 时，$σ \approx 8-9\%$）的经验假设。该公式重现了物理可观测物理量——体积积分和汤川耦合——验证了符号蒸馏能够为此前仅能通过黑盒网络获取的物理量恢复出紧凑、可解释的模型。

## 速览
**TLDR**：本研究针对弦理论中难以计算的卡拉比-丘流形度规，提出了一种基于符号回归的蒸馏方法。通过将复杂的神经网络近似模型转化为简洁、可解释的数学公式，研究者仅用五个项就达到了与神经网络相当的精度（R²=0.9994），且参数量减少了3000倍。该公式不仅能跨模空间保持稳定，还能准确复现体积积分和汤川耦合等物理观测值，为复杂几何量的解析表达提供了新途径。 \
**Motivation**：旨在解决卡拉比-丘流形度规计算困难且神经网络模型缺乏可解释性的问题。 \
**Method**：利用符号回归技术从预训练的神经网络教师模型中提取简洁的解析表达式。 \
**Result**：得到的五项公式在极低参数量下实现了极高精度，并成功预测了关键物理观测值。 \
**Conclusion**：符号蒸馏能有效将黑盒模型转化为紧凑、可解释的物理模型，为几何物理研究提供了有力工具。

---

## Abstract
Calabi--Yau manifolds are essential for string theory but require computing intractable metrics. Here we show that symbolic regression can distill neural approximations into simple, interpretable formulas. Our five-term expression matches neural accuracy ($R^2 = 0.9994$) with 3,000-fold fewer parameters. Multi-seed validation confirms that geometric constraints select essential features, specifically power sums and symmetric polynomials, while permitting structural diversity. The functional form can be maintained across the studied moduli range ($ψ\in [0, 0.8]$) with coefficients varying smoothly; we interpret these trends as empirical hypotheses within the accuracy regime of the locally-trained teachers ($σ\approx 8-9\%$ at $ψ\neq 0$). The formula reproduces physical observables -- volume integrals and Yukawa couplings -- validating that symbolic distillation recovers compact, interpretable models for quantities previously accessible only to black-box networks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在弦理论中，计算卡拉比-丘（Calabi-Yau, CY）流形的 Ricci 平坦度规（Ricci-flat metrics）是理解粒子物理性质（如汤川耦合）的关键。然而，求解定义度规的 Monge-Ampère 方程在计算上是极其困难的。
*   **研究动机**：虽然近年来神经网络（NN）作为代理模型在计算速度上取得了突破，但其“黑盒”性质导致模型缺乏物理可解释性。本研究旨在探索：**度规观测值是否存在紧凑、可解释的解析形式，既能保持神经网络的精度，又能揭示几何结构随模空间（Moduli Space）变化的规律？**

### 2. 论文提出的方法论
核心思想是**符号蒸馏（Symbolic Distillation）**，即利用符号回归从高性能神经网络“教师”模型中提取简洁的数学公式。
*   **教师模型**：使用 Donaldson 算法训练的代数度规（H-矩阵），在 Fermat 五次空间上达到 $0.65\%$ 的 Ricci 平坦误差（$\sigma$）。
*   **回归目标**：学习度规行列式与 Fubini-Study 度规行列式之比的对数：$y = \log(\frac{\det g_{alg}}{\det g_{FS}})$。
*   **特征工程**：构建了规范不变的几何不变量，包括幂和（Power Sum）$p_2$ 和第三初等对称多项式 $\sigma_3$。
*   **符号回归工具**：使用 **PySR** 库，通过帕累托（Pareto）准则平衡公式复杂度和拟合损失。
*   **核心公式**：最终发现了一个仅含 5 个项的解析表达式：
    $$\log \frac{\det g_{alg}}{\det g_{FS}} = c_0 + c_1 \frac{1}{p_2^2} + c_2 \frac{\sigma_3}{p_2^2} + c_3 p_2 + c_4 \sigma_3$$
    其中系数 $c_i$ 随模参数 $\psi$ 平滑变化。

### 3. 实验设计
*   **数据集与场景**：
    *   主要针对 **Dwork 家族五次超曲面**，模参数范围 $\psi \in [0, 0.8]$。
    *   在每个 $\psi$ 点采样 $10^5$ 个点进行训练和验证。
*   **Benchmark（基准）**：
    *   **Donaldson H-matrix**（教师模型，875 个参数）。
    *   **Ashmore (2021) 神经网络**（约 15,000 个参数）。
    *   **三阶多项式基准**（10 个参数）。
*   **物理验证**：通过计算体积积分（Volume Integrals）和汤川耦合（Yukawa Couplings）来验证公式的物理保真度。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 GPU**（通过 Google Colab Pro 访问）。
*   **训练细节**：符号回归运行了 200 次迭代，种群大小为 60。
*   **效率对比**：符号公式在推理速度上比神经网络快 3,000 倍，比 Donaldson 算法快 10,000 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **多点验证**：在 $\psi = \{0.0, 0.2, 0.4, 0.6, 0.8\}$ 多个模空间点独立训练。
    *   **稳定性测试**：进行了 **10 个随机种子** 的独立符号回归运行，验证特征（$p_2, \sigma_3$）的鲁棒性。
    *   **消融实验**：通过移除 $\sigma_3$ 验证其对捕捉各向异性的必要性（误差降低了近 50%）。
    *   **跨阶数验证**：在多项式阶数 $k=6, 8, 10$ 下验证了公式结构的稳定性。
*   **评价**：实验设计较为充分，包含了统计显著性检验（置换检验 $p < 0.001$）和留一种子交叉验证，结果具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **极高压缩比**：5 项公式实现了 **3,000 倍** 的参数缩减，同时保持了 $R^2 = 0.9994$ 的拟合精度。
*   **因子分解原理**：发现度规结构遵循“固定功能支架 + 模相关系数”的模式。即函数形式在模空间内保持不变，而几何变形完全由平滑变化的系数编码。
*   **分层调制响应**：系数随 $\psi$ 的变化呈现分层规律：奇异项（$c_1, c_3$）发生符号反转，而对称项（$c_4$）单调增强（增强了 15 倍）。
*   **物理一致性**：公式成功复现了汤川耦合（误差 $<0.001\%$）和体积积分（误差 $\approx 2\%$）。

### 7. 优点
*   **可解释性**：将复杂的黑盒模型转化为具有明确几何意义（径向各向异性、角结构）的解析项。
*   **发现新物理规律**：通过符号回归自动发现了多项式拟合无法捕捉的奇异项（$1/p_2^n$），这与轨道折叠（Orbifold）渐近几何的预期一致。
*   **极高性能**：微秒级的评估速度为大规模扫描模空间以寻找符合物理观测的弦真空提供了可能。

### 8. 不足与局限
*   **教师模型依赖**：符号公式的精度上限受限于教师模型。在 $\psi \neq 0$ 时，教师模型的 Ricci 误差增加（$8-9\%$），这可能掩盖了某些细微的几何特征。
*   **覆盖范围有限**：实验仅限于 Dwork 五次家族，尚未验证其在更高维模空间或其他 CY 流形家族（如双三次流形）上的通用性。
*   **目标单一**：目前仅针对度规行列式比（标量）进行了建模，尚未直接给出完整的度规张量（Tensor）解析形式。
*   **经验性结论**：关于系数随模参数变化的规律目前仍是经验性的观察，缺乏严格的数学证明。

（完）
