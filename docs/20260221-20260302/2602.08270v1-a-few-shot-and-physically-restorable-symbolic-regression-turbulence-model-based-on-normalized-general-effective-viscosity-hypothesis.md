---
title: A few-shot and physically restorable symbolic regression turbulence model based on normalized general effective-viscosity hypothesis
title_zh: 基于归一化广义有效粘性假设的小样本且物理可还原的符号回归湍流模型
authors: "Ziqi Ji, Penghao Duan, Gang Du"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08270v1"
tags: ["query:sr"]
score: 10.0
evidence: 用于工程的符号回归湍流模型
tldr: 针对RANS湍流模型精度不足及现有数据驱动模型泛化性差的问题，本文提出一种基于归一化广义有效粘度假设的少样本、物理可恢复符号回归湍流模型。该模型仅需少量特定流动数据训练，即可在多种复杂流动场景中超越基准模型，并能在特定物理条件下回归至基准模型，兼具高精度与物理一致性。
selection_source: fresh_fetch
motivation: 解决传统RANS模型精度低以及现有数据驱动湍流模型在泛化性和物理可解释性方面的局限。
method: 基于归一化广义有效粘度假设，利用少量三维周期山流动数据进行符号回归训练，构建具有物理可恢复性的湍流模型。
result: 该模型在周期山、NACA0012机翼及NASA Rotor 37压气机等多种流动测试中表现优于基准模型。
conclusion: 符号回归结合物理约束能有效提升湍流模型的少样本学习能力和跨场景泛化性能，同时保持物理合理性。
---

## 摘要
湍流是自然过程和工程应用中普遍存在的一种复杂、不规则的流动现象。雷诺平均纳维-斯托克斯（RANS）方法由于其计算成本低，已成为快速模拟工程湍流问题的主要方法。然而，经典湍流模型的不准确性构成了 RANS 框架的主要缺点。随着数据驱动方法的快速发展，许多数据驱动的湍流模型被提出，但它们仍面临泛化性和准确性的问题。在这项工作中，我们提出了一种基于归一化广义有效粘性假设的小样本、物理可还原的符号回归湍流模型。小样本表示我们的模型仅在涵盖窄范围湍流物理特性的有限流动构型上进行训练，但仍能在显著不同的湍流中优于基准模型。物理可还原意味着仅使用符号回归训练结果，我们的模型在满足特定物理条件的区域内可以近乎还原为基准模型。归一化广义有效粘性假设是在我们之前的研究中提出的。具体而言，我们首先形式化了小样本数据驱动湍流模型的概念。其次，我们仅使用三维周期山流动切片的直接数值模拟（DNS）数据来训练我们的符号回归湍流模型。第三，我们在周期山流动、零压力梯度平板流动、NACA0012 翼型流动以及 NASA Rotor 37 跨声速轴流压气机流动上评估了我们的模型。我们的其中一个符号回归湍流模型始终优于基准模型，并且我们进一步证明了该模型在某些流动区域可以近乎还原为基准行为。

## 速览
**TLDR**：针对RANS湍流模型精度不足及数据驱动模型泛化性差的问题，本研究提出一种基于归一化通用有效粘度假设的符号回归湍流模型。该模型仅需少量DNS数据训练，即可在多种复杂流动场景中超越基准模型，并具备在特定物理条件下还原为基准模型的物理可恢复性，显著提升了数据驱动模型的泛化能力与物理一致性。 \
**Motivation**：旨在解决传统RANS模型精度受限以及现有数据驱动湍流模型在不同流动工况下泛化性与物理可解释性不足的问题。 \
**Method**：基于归一化通用有效粘度假设，利用少量三维周期山流动切片的DNS数据进行符号回归训练，构建具有物理可恢复性的湍流模型。 \
**Result**：所提模型在周期山、NACA0012机翼及NASA Rotor 37跨声速压缩机等多种异构流动测试中均表现出优于基准模型的性能，并成功实现了物理还原。 \
**Conclusion**：该研究证明了结合物理假设的符号回归方法能以极小样本量构建出高泛化性且符合物理规律的湍流模型，为工程湍流模拟提供了新思路。

---

## Abstract
Turbulence is a complex, irregular flow phenomenon ubiquitous in natural processes and engineering applications. The Reynolds-averaged Navier-Stokes (RANS) method, owing to its low computational cost, has become the primary approach for rapid simulation of engineering turbulence problems. However, the inaccuracy of classical turbulence models constitutes the main drawback of the RANS framework. With the rapid development of data-driven approaches, many data-driven turbulence models have been proposed, yet they still suffer from issues of generalizability and accuracy. In this work, we propose a few-shot, physically restorable, symbolic regression turbulence model based on the normalized general effective-viscosity hypothesis. Few-shot indicates that our model is trained on limited flow configurations spanning only a narrow subset of turbulent flow physics, yet can still outperform the baseline model in substantially different turbulent flows. Physically restorable means our model can nearly revert to the baseline model in regimes satisfying specific physical conditions, using only the symbolic regression training results. The normalized general effective-viscosity hypothesis was proposed in our previous study. Specifically, we first formalize the concept of few-shot data-driven turbulence models. Second, we train our symbolic regression turbulence models using only direct numerical simulation (DNS) data for three-dimensional periodic hill flow slices. Third, we evaluate our models on periodic hill flows, zero pressure gradient flat plate flow, NACA0012 airfoil flows, and NASA Rotor 37 transonic axial compressor flows. One of our symbolic regression turbulence models consistently outperforms the baseline model, and we further demonstrate that this model can nearly revert to baseline behavior in certain flow regimes.

---

## 论文详细总结（自动生成）

这是一份关于论文《A few-shot and physically restorable symbolic regression turbulence model based on normalized general effective-viscosity hypothesis》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的雷诺平均纳维-斯托克斯（RANS）湍流模型在处理复杂流动（如分离流、跨声速流）时精度不足，而现有的数据驱动模型往往面临泛化性差（换个场景就失效）和“黑盒”特性（缺乏物理可解释性）的问题。
*   **研究背景**：研究者希望开发一种既能利用数据提升精度，又能在特定物理条件下回归到经典模型（物理可还原性），且仅需极少量特定流动数据训练即可推广到复杂工程场景（小样本学习）的湍流模型。

### 2. 论文提出的方法论
*   **核心思想**：基于**归一化广义有效粘性假设**，利用**符号回归（Symbolic Regression）**自动从高保真数据中提取显式的代数雷诺应力修正公式。
*   **关键技术细节**：
    *   **归一化假设**：对 Pope (1975) 提出的张量基底进行归一化处理，使各基底系数的绝对值直接反映其对雷诺应力各向异性的贡献程度，增强了模型的数值稳定性和泛化能力。
    *   **符号回归（PySR）**：使用进化算法搜索数学表达式。为了保证物理可还原性，模型固定了线性项（对应经典 Boussinesq 假设），仅对非线性项进行符号回归建模。
    *   **特征工程**：输入特征包括 7 个张量不变量（反映应变率、旋转率、压力梯度等）和 5 个额外物理特征（如壁面距离雷诺数、湍流时间尺度比等）。
    *   **物理还原机制**：通过训练使模型在特定物理极限下（如近壁面粘性底层或各向同性衰减区），非线性修正项的系数趋于零，从而自动退化为基准的 $k-\omega$-SST 模型。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：仅使用三维周期山（Periodic Hill）流动的 DNS 数据切片（几何参数 $\alpha=0.8$ 和 $1.2$）。
    *   **测试集**：周期山外推实验（$\alpha=0.5, 1.0, 1.5$）、零压力梯度平板流动、NACA0012 翼型流动、NASA Rotor 37 跨声速轴流压气机。
*   **Benchmark（基准）**：经典 $k-\omega$-SST 湍流模型。
*   **对比方法**：
    *   **SR 3T**：仅包含 2D 流动线性无关的 3 个张量基底的符号回归模型。
    *   **SR 5T**：包含 5 个张量基底的符号回归模型（本文推荐模型）。
    *   **SA-NN**：针对 Rotor 37 案例，对比了前人直接用该案例实验数据训练的神经网络模型。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件型号（如 GPU/CPU 数量）及总训练时长。
*   **算法参数**：文中详细列出了符号回归库 PySR 的超参数，包括 16 个种群、每个种群 100 个个体、2000 次迭代、最大方程复杂度为 20 等。这表明该方法在训练阶段对计算资源有一定要求，但一旦生成显式方程，推理阶段的计算成本与传统 RANS 模型相当。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了从低雷诺数不可压流动到高雷诺数跨声速可压流动的 4 类典型场景。
*   **充分性评价**：实验设计较为充分且具有挑战性。特别是“小样本”特性的验证非常严格——仅用简单的周期山数据训练，却在复杂的旋转机械（Rotor 37）上取得了成功。
*   **客观性**：通过对比 SR 3T 和 SR 5T，客观展示了张量基底完整性对模型性能的影响；同时通过平板流动验证了模型不会破坏经典模型在简单流动中的准确性。

### 6. 主要结论与发现
*   **性能卓越**：SR 5T 模型在所有测试案例中均优于基准 $k-\omega$-SST 模型。在 Rotor 37 案例中，其预测的绝热效率甚至优于直接用该案例数据训练的神经网络模型。
*   **物理一致性**：证明了符号回归模型能够自动学习到物理还原行为。例如，在平板流动的粘性底层，非线性项自动失活，模型回归到 Boussinesq 假设。
*   **关键项发现**：研究发现第 6 个张量基底（$\hat{\mathbf{T}}_6$）在预测分离流中起到了至关重要的作用，这是 SR 5T 优于 SR 3T 的主要原因。

### 7. 优点
*   **强泛化能力**：实现了真正意义上的“小样本学习”，跨越了流动构型和物理机制（不可压到可压）的鸿沟。
*   **显式可解释性**：生成的模型是简单的数学公式，易于植入任何 CFD 软件，且物理意义可通过项的组合进行分析。
*   **物理还原性**：无需人工干预（如屏蔽函数），模型能根据局部流动特征自动判断是否回归基准模型，保证了在标准工况下的可靠性。

### 8. 不足与局限
*   **超参数依赖**：符号回归的超参数（如复杂度惩罚、算子选择）目前仍依赖专家经验，缺乏系统性的优化标准。
*   **局部过拟合风险**：在 NASA Rotor 37 的上游区域，模型表现出一定的过拟合迹象（预测了不应存在的非线性修正），这说明小样本训练虽然泛化性强，但在某些极端特征区域仍可能产生偏差。
*   **训练成本**：符号回归的搜索空间巨大，虽然文中未详述时长，但通常此类进化算法的训练耗时远高于简单的回归任务。

（完）
