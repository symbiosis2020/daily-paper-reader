---
title: "GENSR: Symbolic Regression Based in Equation Generative Space"
title_zh: GENSR：基于方程生成空间的符号回归
authors: "Qian Li, Yuxiao Hu, Juncheng Liu, Yuntian Chen"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20557v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于生成潜空间的符号回归框架
tldr: 符号回归（SR）旨在从数据中发现数学方程，但传统方法在离散空间搜索，导致反馈信号嘈杂。本文提出GenSR框架，通过预训练条件变分自编码器（CVAE）构建具有数值平滑性的生成式潜空间。该方法遵循“地图构建-粗略定位-精细搜索”范式，利用CVAE定位潜力区域并结合CMA-ES进行精细优化。GenSR在预测精度、表达式简洁度和计算效率上表现优异，且具备较强的抗噪能力。
selection_source: fresh_fetch
motivation: 传统符号回归在离散空间搜索，方程结构微调与数值行为不一致，导致搜索反馈信号过于嘈杂且难以引导探索。
method: 提出基于CVAE的生成式潜空间框架，将方程重参数化为连续平滑的潜空间，并结合CMA-ES算法进行精细化搜索。
result: 实验表明GenSR在预测准确性、公式简洁性和计算效率方面均表现出色，并在噪声环境下保持了较强的鲁棒性。
conclusion: GenSR通过将符号回归转化为潜空间优化问题，为解决离散方程搜索难题提供了有效的理论保证和实践路径。
---

## 摘要
符号回归 (SR) 旨在揭示观测数据背后隐藏的方程。然而，大多数方法在离散方程空间中进行搜索，其中方程的结构修改很少与其数值行为一致，导致拟合误差反馈过于嘈杂，难以引导探索。为了应对这一挑战，我们提出了 GenSR，这是一个基于生成潜空间的符号回归框架，遵循“地图构建 -> 粗略定位 -> 精细搜索”的范式。具体而言，GenSR 首先预训练一个双分支条件变分自编码器 (CVAE)，将符号方程重新参数化为具有符号连续性和局部数值平滑性的生成潜空间。该空间可以被视为方程空间中结构良好的“地图”，为搜索提供方向信号。在推理阶段，CVAE 将输入数据粗略定位到潜空间中有潜力的区域。随后，改进的 CMA-ES 利用平滑的潜梯度对候选区域进行精细化处理。从贝叶斯角度来看，GenSR 将符号回归任务重新定义为最大化条件分布 $p(\mathrm{Equ.} \mid \mathrm{Num.})$，而 CVAE 的训练通过证据下界 (ELBO) 实现了这一目标。这一新视角为 GenSR 的有效性提供了理论保证。大量实验表明，GenSR 在保持对噪声鲁棒性的同时，协同优化了预测准确性、表达式简洁性和计算效率。

## Abstract
Symbolic Regression (SR) tries to reveal the hidden equations behind observed data. However, most methods search within a discrete equation space, where the structural modifications of equations rarely align with their numerical behavior, leaving fitting error feedback too noisy to guide exploration. To address this challenge, we propose GenSR, a generative latent space-based SR framework following the `map construction -> coarse localization -> fine search'' paradigm. Specifically, GenSR first pretrains a dual-branch Conditional Variational Autoencoder (CVAE) to reparameterize symbolic equations into a generative latent space with symbolic continuity and local numerical smoothness. This space can be regarded as a well-structured `map'' of the equation space, providing directional signals for search. At inference, the CVAE coarsely localizes the input data to promising regions in the latent space. Then, a modified CMA-ES refines the candidate region, leveraging smooth latent gradients. From a Bayesian perspective, GenSR reframes the SR task as maximizing the conditional distribution $p(\mathrm{Equ.} \mid \mathrm{Num.})$, with CVAE training achieving this objective through the Evidence Lower Bound (ELBO). This new perspective provides a theoretical guarantee for the effectiveness of GenSR. Extensive experiments show that GenSR jointly optimizes predictive accuracy, expression simplicity, and computational efficiency, while remaining robust under noise.

---

## 论文详细总结（自动生成）

这是一份关于论文《GENSR: Symbolic Regression Based in Equation Generative Space》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的符号回归（SR）主要在**离散的方程空间**中搜索（如遗传算法、树搜索）。在这种空间里，方程结构的微小改变（编辑距离）往往导致数值行为的剧烈跳变，使得拟合误差反馈极其嘈杂，无法有效引导搜索方向。
*   **研究动机**：如何构建一个**连续且生成式的潜空间**，将离散的方程重参数化，使得空间既具备符号结构的连续性，又具备局部数值行为的平滑性，从而将符号回归转化为高效的连续空间优化问题。

### 2. 方法论：GenSR 框架
GenSR 遵循“地图构建 → 粗略定位 → 精细搜索”的范式：
*   **核心思想**：利用双分支条件变分自编码器（CVAE）构建方程的“地图”。
    *   **后验分支**：输入数值样本和符号方程，学习符号结构的分布，确保潜空间的**符号连续性**。
    *   **先验分支**：仅输入数值样本，通过 KL 散度与后验分支对齐，捕捉数值特征，确保潜空间的**局部数值平滑性**。
*   **关键技术细节**：
    *   **贝叶斯视角**：将 SR 任务建模为最大化条件分布 $p(Equ. | Num.)$，证明了 CVAE 的训练本质上是在优化证据下界（ELBO）。
    *   **精细搜索算法**：在推理阶段，先由先验网络给出初始定位，再使用**改进的 CMA-ES（协方差矩阵自适应进化策略）**。改进点包括：采用对角协方差假设以降低复杂度，以及仅更新方差最大的前 $k$ 个维度（Top-k 策略）以加速收敛。
    *   **常量优化**：在解码出方程后，使用 BFGS 算法对其中的常数进行精细微调。

### 3. 实验设计
*   **数据集/场景**：
    *   **Feynman Benchmark**：包含 119 个来自物理学讲义的经典方程。
    *   **Strogatz Benchmark**：14 个来自非线性动力系统的挑战任务。
    *   **Black-box Dataset**：57 个来自 PMLB 库的真实世界黑盒回归任务（真实方程未知）。
*   **对比方法**：涵盖了 18 种基准算法，包括：
    *   **启发式/遗传算法**：GPlearn, Operon, GP-GOMEA, AFP 等。
    *   **深度学习/强化学习方法**：DSR, E2ESR, SNIP, TPSR, RAG-SR, MDLformer 等。
    *   **物理/概率启发方法**：AI Feynman 2.0, BSR 等。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H800 GPU**（Hopper 架构，每台 80GB 显存）。
*   **训练细节**：在约 500 万个合成方程-样本对上进行预训练。
*   **时长**：训练 200 个 epoch，每个 epoch 耗时约 1 小时，总训练时间约为 **200 小时**。
*   **模型规模**：模型包含约 **1.621 亿**个可训练参数。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的基准测试，涵盖了不同噪声水平（0 到 0.1）、不同输入维度（最高 10 维）以及不同复杂度的方程。
*   **消融实验**：详细探讨了潜空间维度、CMA-ES 的种群大小、步长、Top-k 维度选择以及后验分支必要性的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了精度（$R^2$），还引入了**帕累托前沿（Pareto Front）**来综合评估精度、方程复杂度和计算时间之间的权衡，并提供了 t-SNE 可视化来验证潜空间的性质。

### 6. 主要结论与发现
*   **性能领先**：GenSR 在 Feynman 和 Strogatz 数据集上始终处于帕累托前沿的第一梯队，在精度、简洁度和效率之间达到了最佳平衡。
*   **抗噪性强**：在存在目标噪声的情况下，GenSR 的 $R^2$ 分数下降最慢，且方差较小，表现出极强的鲁棒性。
*   **空间优越性**：可视化证明了 GenSR 构建的潜空间能够清晰地分离不同的函数族（如三角函数、对数函数），并保持了数值特征的平滑过渡。
*   **泛化能力**：通过函数分类任务证明了该潜空间学习到的特征具有很强的迁移能力。

### 7. 优点与亮点
*   **范式创新**：打破了离散搜索的局限，通过 CVAE 将符号回归转化为连续空间的“定位+搜索”问题。
*   **理论完备**：从贝叶斯推断和 ELBO 优化的角度为框架提供了坚实的数学支撑。
*   **效率优化**：针对高维潜空间改进了 CMA-ES，使其在保持精度的同时显著降低了搜索耗时。
*   **生成性**：相比 SNIP 等判别式方法，GenSR 的空间是生成式的，支持平滑插值，减少了“无效方程区域”。

### 8. 不足与局限
*   **预训练依赖**：模型性能高度依赖于合成数据集的质量和覆盖范围，对于完全超出预训练分布的极端复杂方程，可能仍面临挑战。
*   **计算开销**：虽然推理搜索很快，但前期的预训练需要昂贵的 H800 算力和大量数据。
*   **维度限制**：目前实验主要集中在 10 维以内的输入变量，对于超高维特征的科学发现场景，其表现尚待验证。
*   **算子集固定**：潜空间的构建受限于预定义的算子集（如 sin, exp, log 等），扩展新算子可能需要重新训练模型。

（完）
