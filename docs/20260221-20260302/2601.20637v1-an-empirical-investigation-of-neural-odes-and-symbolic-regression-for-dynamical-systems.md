---
title: An Empirical Investigation of Neural ODEs and Symbolic Regression for Dynamical Systems
title_zh: 神经常微分方程与符号回归在动力系统中的实证研究
authors: "Panayiotis Ioannou, Pietro Liò, Pietro Cicuta"
date: 2026-01-28
pdf: "https://arxiv.org/pdf/2601.20637v1"
tags: ["query:sr"]
score: 10.0
evidence: 符号回归（SR）从噪声数据中恢复底层方程
tldr: 本研究探讨了利用神经常微分方程（NODEs）和符号回归（SR）建模复杂动力系统并发现其控制方程的方法。通过对阻尼振荡系统的实验，研究发现NODEs在动力学相似的情况下具有良好的外推能力，而SR能有效从噪声数据中恢复方程。最重要的是，即使在训练数据极少的情况下，利用NODE生成的数据也能辅助SR推断出物理定律，为科学发现提供了新路径。
selection_source: fresh_fetch
motivation: 准确建模复杂动力系统并发现其背后的控制微分方程是加速科学发现的关键任务。
method: 通过阻尼振荡系统的噪声合成数据，评估了神经常微分方程的外推性能以及符号回归恢复底层方程的能力。
result: "NODEs在相似动力学条件下外推效果显著，且SR能从仅基于10%原始数据训练的NODE所生成的数据中成功恢复大部分方程。"
conclusion: 结合NODEs增强有限数据与符号回归推断物理定律的方法，为科学探索和规律发现提供了一种极具前景的新策略。
---

## 摘要
准确建模复杂系统的动力学并发现其控制微分方程是加速科学发现的关键任务。利用来自两个阻尼振荡系统的带噪声合成数据，我们探索了神经常微分方程（NODEs）的外推能力以及符号回归（SR）恢复底层方程的能力。我们的研究得出了三个关键见解。首先，我们证明了只要生成的轨迹与训练数据具有动力学相似性，NODEs 就能有效地外推到新的边界条件。其次，SR 成功地从带噪声的真实数据中恢复了方程，尽管其性能取决于输入变量的正确选择。最后，我们发现，当使用仅在完整模拟的 10% 数据上训练的 NODE 生成的数据时，SR 恢复了三个控制方程中的两个，并对第三个方程给出了良好的近似。虽然最后这一发现指出了未来工作的方向，但我们的结果表明，利用 NODEs 丰富有限的数据并使符号回归能够推断物理定律，代表了科学发现的一种极具前景的新方法。

## 速览
**TLDR**：本研究探讨了神经常微分方程（NODEs）与符号回归（SR）在动力系统建模中的结合应用。通过对阻尼振荡系统的实验，研究发现NODE在具有动力学相似性的边界条件下表现出良好的外推能力，而SR能从噪声数据中恢复控制方程。最重要的是，研究证明了利用训练好的NODE生成合成数据来辅助SR推导物理定律的可行性，为数据稀缺场景下的科学发现提供了新路径。 \
**Motivation**：旨在解决复杂动力系统建模中，如何从有限且含噪声的数据中准确恢复控制方程并实现长期预测的挑战。 \
**Method**：通过在两个阻尼振荡系统上对比NODE的外推性能，并评估SR从原始噪声数据及NODE生成的合成数据中提取物理定律的能力。 \
**Result**：NODE能有效外推至相似动力学条件，且SR利用仅由10%原始数据训练的NODE所生成的合成数据，成功恢复了大部分控制方程。 \
**Conclusion**：将NODE作为数据增强工具与SR结合，是推断物理定律并加速科学发现的一种极具潜力的新方法。

---

## Abstract
Accurately modelling the dynamics of complex systems and discovering their governing differential equations are critical tasks for accelerating scientific discovery. Using noisy, synthetic data from two damped oscillatory systems, we explore the extrapolation capabilities of Neural Ordinary Differential Equations (NODEs) and the ability of Symbolic Regression (SR) to recover the underlying equations. Our study yields three key insights. First, we demonstrate that NODEs can extrapolate effectively to new boundary conditions, provided the resulting trajectories share dynamic similarity with the training data. Second, SR successfully recovers the equations from noisy ground-truth data, though its performance is contingent on the correct selection of input variables. Finally, we find that SR recovers two out of the three governing equations, along with a good approximation for the third, when using data generated by a NODE trained on just 10% of the full simulation. While this last finding highlights an area for future work, our results suggest that using NODEs to enrich limited data and enable symbolic regression to infer physical laws represents a promising new approach for scientific discovery.

---

## 论文详细总结（自动生成）

这篇论文对神经常微分方程（Neural ODEs, NODEs）与符号回归（Symbolic Regression, SR）在动力系统建模中的结合进行了深入的实证研究。以下是详细的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在科学发现中，从实验数据中推断控制系统的微分方程至关重要。然而，现实数据往往具有**高噪声、稀疏性**且难以解释。
*   **核心问题**：
    1.  NODE 在面对噪声数据时的插值与外推能力如何？
    2.  能否利用 NODE 作为“数据增强”工具，在原始数据极度匮乏的情况下，辅助符号回归（SR）恢复底层物理定律？
*   **整体含义**：论文提出并验证了一种“NODE + SR”的流水线，旨在利用 NODE 的连续建模能力弥补 SR 对高质量、大规模数据的依赖，为数据稀缺领域的科学发现提供新路径。

### 2. 方法论
*   **核心思想**：将 NODE 作为黑盒动力学学习器，学习系统的演化规律，然后利用训练好的 NODE 生成密集、去噪的合成数据，最后由 SR 从中提取可解释的数学公式。
*   **关键技术细节**：
    *   **神经常微分方程 (NODE)**：使用多层感知机（MLP）拟合导数函数 $f(z(t), t, \theta)$，通过 ODE 求解器进行前向传播。
    *   **符号回归 (SR)**：采用 **PySR** 库，通过进化算法在数学算子空间中搜索最匹配数据的解析表达式。
    *   **数据增强流水线**：仅使用 10% 的原始模拟数据训练 NODE，随后利用该模型生成完整的轨迹数据供 SR 分析。
*   **公式背景**：研究涉及两个系统：
    1.  **倒立摆 (Cart-pole)**：经典的二阶非线性物理系统。
    2.  **生物模型 (Bio-model)**：描述大肠杆菌在营养环境变化下的适应性，包含三个状态变量（氨基酸比例、核糖体质量分数、基因调节函数）。

### 3. 实验设计
*   **场景与数据集**：
    *   **倒立摆系统**：在不同初始角度和角速度下生成合成数据，加入 -5% 到 5% 的均匀分布噪声。
    *   **生物模型**：模拟营养物质的“上调”和“下调”过程，涉及复杂的有理函数项。
*   **Benchmark 与对比**：
    *   对比了 SR 在**原始无噪声数据**、**原始带噪声数据**以及 **NODE 生成数据**上的表现。
    *   评估了不同采样频率（每小时 5 到 100 个点）对 NODE 预测精度的影响。

### 4. 资源与算力
*   **软件框架**：使用了基于 JAX 的 **Diffrax** 库进行 NODE 训练，**PySR** 进行符号搜索。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。但提到 NODE 训练了 100,000 到 150,000 次迭代，SR 运行了 800 次迭代。考虑到模型规模（2 层隐藏层，每层 20 个节点），此类实验通常在单张消费级 GPU 或高性能 CPU 上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对倒立摆，测试了 35 种边界条件。
    *   针对生物模型，测试了 19 种不同的营养转换场景，并对每个采样频率进行了 10 次独立运行以确保统计显著性。
*   **充分性评价**：实验设计较为客观，涵盖了物理和生物两个跨学科领域。通过热图分析了相空间轨迹与外推误差的关系，验证了动力学相似性是外推成功的关键，实验逻辑严密。

### 6. 主要结论与发现
*   **NODE 的外推特性**：NODE 的外推能力取决于新初始条件是否与训练数据处于**相同的相空间轨迹**上。只要动力学特性相似，NODE 甚至能预测训练时长 5 倍以外的演化。
*   **采样频率的影响**：长期预测（8小时）对采样频率不敏感，但极低频率（稀疏数据）会显著增加短期插值误差，因为噪声在稀疏点中会被放大。
*   **NODE 作为去噪器**：SR 直接处理 5% 噪声数据时表现极差，但处理由 NODE 生成的数据时，成功恢复了 3 个方程中的 2 个。这表明 **NODE 具有天然的去噪和数据平滑功能**。
*   **变量选择的重要性**：SR 的成功高度依赖于输入变量的选择（如是否包含中间变量 $\lambda$）。

### 7. 优点
*   **创新流水线**：成功展示了 NODE 如何作为 SR 的前置处理器，解决 SR 在噪声和稀疏数据下的脆弱性。
*   **跨领域验证**：在物理系统和复杂的生物化学模型上均取得了成功，证明了方法的通用性。
*   **深入的动力学解释**：不仅给出了误差曲线，还通过相空间分析解释了深度学习模型为何能（或不能）外推。

### 8. 不足与局限
*   **输入依赖性**：SR 仍需人工指定可能的输入变量，若缺少关键中间变量（如 $\lambda$），SR 难以发现复杂的有理项。
*   **复杂项丢失**：在生物模型中，SR 未能完全恢复最复杂的方程（方程 2），虽然得到了良好近似，但丢失了信号较弱的项。
*   **实验覆盖面**：SR 分析目前主要集中在单次转换模拟，未来需要测试更多样化的多条件组合数据以增强鲁棒性。

（完）
