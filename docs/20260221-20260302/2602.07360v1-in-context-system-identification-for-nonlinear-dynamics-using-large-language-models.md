---
title: In-Context System Identification for Nonlinear Dynamics Using Large Language Models
title_zh: 基于大语言模型的非线性动力学上下文系统辨识
authors: Linyu Lin
date: 2026-02-07
pdf: "https://arxiv.org/pdf/2602.07360v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用LLM辅助SINDy从数据中发现简约的控制方程
tldr: 本研究提出一种LLM辅助的SINDy流水线，旨在解决非线性动力学识别中候选库依赖专家调优的难题。通过将LLM引入迭代循环，利用上下文学习根据误差反馈和领域约束优化方程结构。在63个动力学数据集及核反应堆模型上的实验证明，该方法在符号恢复准确度和预测精度上均优于传统SINDy，实现了数据驱动与符号推理的有效结合。
selection_source: fresh_fetch
motivation: 传统的非线性动力学稀疏识别（SINDy）方法高度依赖专家经验来手动构建和调优候选函数库。
method: 提出一种将LLM引入循环的SINDy流水线，通过上下文学习迭代地根据误差反馈和领域约束优化方程结构。
result: 在63个动力学基准数据集及核反应堆模型上，该方法在符号恢复相似度和测试均方根误差方面均优于基准SINDy。
conclusion: 研究证明LLM能有效引导方程空间搜索，通过整合数据反馈与符号推理，发现更准确且具解释性的动力学方程。
---

## 摘要
非线性动力学稀疏识别（SINDy）是从数据中发现简洁控制方程的强大方法，但它通常需要专家对候选库进行调优。我们提出了一种 LLM 辅助的 SINDy 流水线，通过上下文学习（in-context learning）将大语言模型（LLM）引入循环，迭代地优化候选方程。该流水线首先使用自适应库拟合基准 SINDy 模型，然后进入 LLM 引导的优化循环。在每次迭代中，当前的最佳方程、误差指标和领域特定约束被汇总到 LLM 的提示词中，由其建议新的方程结构。这些候选方程根据定义的符号形式进行解析，并在训练和测试数据上进行评估。该流水线将基于模拟的误差作为主要指标，同时也评估与真实情况（ground truth）的结构相似性，包括匹配函数形式、关键项、耦合关系和定性行为。如果测试误差低于阈值（NRMSE < 0.1）或达到最大 10 次迭代，迭代停止标准将提前结束优化。最后，选择最佳模型，我们在 63 个动力系统数据集（ODEBench）以及用于沸水核反应堆的 March-Leuba 模型上评估了这种 LLM 辅助的 SINDy。结果与经典 SINDy 进行了对比，表明在复杂动力学情况下，LLM 循环一致地提高了符号恢复能力，与基准 SINDy 相比，具有更高的方程结构相似性和更低的测试均方根误差（RMSE）。这项工作证明了 LLM 可以有效地引导 SINDy 在方程空间中的搜索，将数据驱动的误差反馈与受领域启发的符号推理相结合，从而发现不仅准确而且在结构上具有可解释性的控制方程。

## 速览
**TLDR**：针对非线性动力系统识别中SINDy方法依赖专家调整候选库的局限，本文提出一种利用大语言模型（LLM）进行上下文学习的迭代优化框架。该方法将LLM引入SINDy循环，通过提示词整合误差指标与领域约束，引导模型自动搜索并精炼控制方程。在ODEBench等63个数据集上的实验表明，该方法在复杂动力系统的符号恢复准确性、结构可解释性及预测精度上均显著优于传统SINDy。 \
**Motivation**：传统的SINDy方法在处理复杂非线性动力学时，高度依赖人工预设的候选函数库，缺乏自动化的结构优化能力。 \
**Method**：提出一种LLM辅助的SINDy流水线，通过将当前方程、误差反馈和领域约束输入LLM，利用上下文学习迭代生成并验证新的候选方程结构。 \
**Result**：在63个动力系统数据集及核反应堆模型的测试中，该方法在符号相似度和测试均方根误差（RMSE）上均优于基准SINDy。 \
**Conclusion**：LLM能够有效引导符号搜索空间，将数据驱动的误差反馈与符号推理相结合，发现既准确又具可解释性的动力学控制方程。

---

## Abstract
Sparse Identification of Nonlinear Dynamics (SINDy) is a powerful method for discovering parsimonious governing equations from data, but it often requires expert tuning of candidate libraries. We propose an LLM-aided SINDy pipeline that iteratively refines candidate equations using a large language model (LLM) in the loop through in-context learning. The pipeline begins with a baseline SINDy model fit using an adaptive library and then enters a LLM-guided refinement cycle. At each iteration, the current best equations, error metrics, and domain-specific constraints are summarized in a prompt to the LLM, which suggests new equation structures. These candidate equations are parsed against a defined symbolic form and evaluated on training and test data. The pipeline uses simulation-based error as a primary metric, but also assesses structural similarity to ground truth, including matching functional forms, key terms, couplings, qualitative behavior. An iterative stopping criterion ends refinement early if test error falls below a threshold (NRMSE < 0.1) or if a maximum of 10 iterations is reached. Finally, the best model is selected, and we evaluate this LLM-aided SINDy on 63 dynamical system datasets (ODEBench) and march leuba model for boiling nuclear reactor. The results are compared against classical SINDy and show the LLM-loop consistently improves symbolic recovery with higher equation similarity to ground truth and lower test RMSE than baseline SINDy for cases with complex dynamics. This work demonstrates that an LLM can effectively guide SINDy's search through equation space, integrating data-driven error feedback with domain-inspired symbolic reasoning to discover governing equations that are not only accurate but also structurally interpretable.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合大语言模型（LLM）与稀疏识别算法（SINDy）的新型系统辨识框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：从观测数据中发现控制物理系统的控制方程（如 ODE）是科学建模的核心。SINDy 是一种流行的稀疏回归方法，能发现简洁的符号方程。
*   **核心痛点**：传统的 SINDy 高度依赖于人工预定义的“候选函数库”。如果真实的物理项不在库中，或者库过于庞大导致多重共线性，SINDy 往往会失效或产生极其复杂的非物理模型。
*   **研究动机**：利用 LLM 蕴含的广泛科学知识和符号推理能力，替代人类专家来动态建议、优化和筛选候选方程结构，从而实现自动化的“专家级”建模。

### 2. 方法论
该论文提出了一种 **LLM-in-the-loop（LLM 在环）** 的迭代流水线，核心步骤如下：
*   **基准初始化**：首先运行标准 SINDy 获得一个初始模型，并计算训练/测试误差。
*   **数据特征提取**：自动计算轨迹的统计特征（如量程、标准差、单调性、振荡频率、饱和启发式），将其转化为自然语言描述（如“系统呈现阻尼振荡”）。
*   **结构化 Prompt 构建**：向 LLM 提供包含变量元数据、当前方程、误差诊断、数据定性特征、领域先验约束以及历史失败尝试的 Prompt。
*   **受限候选生成**：LLM 建议新的方程结构。要求必须满足线性参数化形式：$\dot{x}_i(t) = \sum \theta_{i,k} \phi_k(x, u, t)$，确保后续可用稀疏回归求解系数。
*   **验证与评估循环**：
    1.  **语法解析**：过滤不符合数学规范或线性约束的建议。
    2.  **稀疏拟合**：在训练集上确定系数。
    3.  **前向模拟**：在测试集上进行数值积分，计算归一化均方根误差（NRMSE）。
*   **多目标评分与停止**：综合考虑预测精度（NRMSE）、符号复杂度（树节点数）和领域惩罚项。若 NRMSE < 0.1 或达到 10 次迭代则停止。

### 3. 实验设计
*   **数据集**：
    *   **ODEBench**：包含 63 个经典的常微分方程系统（涵盖 1D 到 4D），包括捕食者-猎物模型、振荡器、反应动力学及 4 个混沌系统。
    *   **March-Leuba 模型**：一个复杂的 7 变量核反应堆稳定性简化模型。
*   **对比基准（Benchmark）**：经典 SINDy（使用固定且广泛的候选库，包含多项式和三角函数）。
*   **评估指标**：测试集 $R^2$、NRMSE、以及结构恢复质量（定性评估是否捕获了真实的物理项）。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练/推理时长。
*   由于该方法主要涉及 LLM 的文本推理（API 调用）和轻量级的线性回归拟合，其主要的计算开销在于多次迭代中的数值积分（模拟）过程，而非深度学习训练。

### 5. 实验数量与充分性
*   **实验规模**：在 63 个标准基准系统和 1 个复杂的工程实际案例上进行了测试，实验样本量较为充分。
*   **客观性**：涵盖了从简单线性系统到复杂混沌系统的多种动力学行为，对比了数值精度和符号结构相似度，评估维度较为全面。
*   **局限性**：实验主要在无噪声的仿真数据上进行，对于高噪声环境下的表现讨论较少。

### 6. 主要结论与发现
*   **性能飞跃**：在 63 个系统中，LLM 辅助方法在 48 个系统上实现了高精度恢复（NRMSE < 0.1），而基准 SINDy 仅为 30 个。
*   **误差大幅下降**：LLM 辅助方法的中位误差为 3.4%，远低于基准方法的 62%。
*   **结构发现能力**：LLM 能够成功引导搜索超出预定义库的函数（如指数项、复杂的双线性耦合），显著提升了符号恢复的准确性。
*   **稳定性**：基准 SINDy 在 5 个案例中出现模拟发散，而 LLM 辅助流水线通过迭代修正全部找到了稳定的模型。

### 7. 优点与亮点
*   **自动化专家经验**：将 LLM 作为“假设生成器”，模拟了科学家观察误差、调整假设、验证实验的迭代过程。
*   **可解释性与物理一致性**：通过结构先验和符号约束，确保生成的方程不仅准确，而且符合物理直觉，避免了黑盒模型。
*   **自适应库扩展**：突破了固定库的限制，使 SINDy 具备了处理未知函数形式的能力。

### 8. 不足与局限
*   **LLM 偏见风险**：LLM 可能会倾向于它在训练数据中见过的常见方程，对于完全新颖、违背直觉的物理规律可能存在搜索偏见。
*   **计算成本**：相比一键式的 SINDy，迭代循环（尤其是多次数值模拟）显著增加了计算时间。
*   **复杂组合瓶颈**：对于极特殊的非线性组合（如文中提到的 $cot(y)$ 与三角函数的乘积），如果 LLM 未能精准建议该原子项，回归依然会失败。
*   **噪声鲁棒性**：论文未深入探讨在传感器噪声较大时，LLM 如何区分噪声干扰与真实的非线性项。

（完）
