---
title: "Beyond Error-Based Optimization: Experience-Driven Symbolic Regression with Goal-Conditioned Reinforcement Learning"
title_zh: 超越基于误差的优化：基于目标条件强化学习的经验驱动符号回归
authors: "Jianwen Sun, Xinrui Li, Fuqing Li, Xiaoxuan Shen"
date: 2026-01-21
pdf: "https://arxiv.org/pdf/2601.14693v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于经验驱动的目标条件强化学习符号回归
tldr: 针对符号回归中传统误差驱动搜索易陷入局部最优且结构模糊的问题，本文提出EGRL-SR框架。该框架将符号回归建模为目标条件强化学习问题，利用事后经验回放和动作价值网络从历史轨迹中学习映射模式。通过设计全点满足的二元奖励函数和结构引导的启发式探索策略，EGRL-SR能更有效地引导搜索过程，在恢复率和鲁棒性上显著优于现有方法。
selection_source: fresh_fetch
motivation: 传统的基于误差的搜索方法在面对具有相似误差但结构迥异的候选表达式时，难以提供明确的搜索方向，导致收敛困难。
method: 提出一种基于目标条件强化学习的框架，结合事后经验回放、二元奖励函数及结构引导的启发式探索，利用历史经验主动引导搜索。
result: 在公开基准测试中，EGRL-SR在恢复率、鲁棒性以及复杂表达式的恢复能力上均一致优于当前最先进的方法。
conclusion: 实验证明动作价值网络能有效引导搜索，且所设计的奖励函数和探索策略对提升符号回归性能至关重要。
---

## 摘要
符号回归旨在自动识别简洁且可解释的数学表达式，以建模输入和输出变量之间的函数关系。大多数现有的基于搜索的符号回归方法通常依赖拟合误差来指导搜索过程。然而，在广阔的表达式空间中，许多候选表达式可能表现出相似的误差值，但在结构上却存在巨大差异，这导致了模糊的搜索方向，并阻碍了向底层真实函数的收敛。为了应对这一挑战，我们提出了一个名为 EGRL-SR（经验驱动的目标条件强化学习符号回归）的新型框架。与传统的误差驱动方法不同，EGRL-SR 引入了一个新视角：利用精确的历史轨迹并优化动作值网络（action-value network）来主动引导搜索过程，从而实现更稳健的表达式搜索。具体而言，我们将符号回归建模为一个目标条件强化学习问题，并结合了事后经验回放（hindsight experience replay），使动作值网络能够从多样的输入输出对中泛化出通用的映射模式。此外，我们设计了一个全点满足二元奖励函数，鼓励动作值网络关注结构模式而非低误差表达式，并同时提出了一种结构引导的启发式探索策略，以增强搜索的多样性和空间覆盖率。在公共基准测试上的实验表明，EGRL-SR 在恢复率和稳健性方面始终优于现有最先进的方法，并且在相同的搜索预算下可以恢复更复杂的表达式。消融实验结果验证了动作值网络能有效地引导搜索，且奖励函数和探索策略都发挥了关键作用。

## 速览
**TLDR**：针对符号回归中仅依赖拟合误差导致搜索方向模糊的问题，本文提出EGRL-SR框架。该方法将符号回归建模为目标条件强化学习任务，利用历史轨迹和动作价值网络主动引导搜索。通过引入事后经验回放、全点满足奖励函数及结构引导启发式探索，EGRL-SR能从多样化数据中泛化结构模式，显著提升了复杂表达式的恢复率和搜索鲁棒性。 \
**Motivation**：传统符号回归方法过度依赖误差反馈，在海量候选空间中难以区分结构迥异但误差相似的表达式，导致搜索效率低下且难以收敛。 \
**Method**：提出一种基于目标条件强化学习的框架，结合事后经验回放、结构导向的二值奖励函数以及启发式探索策略，利用动作价值网络学习通用的映射模式。 \
**Result**：在公开基准测试中，EGRL-SR在恢复率和鲁棒性上均优于现有最先进方法，且在相同搜索预算下能发现更复杂的数学表达式。 \
**Conclusion**：通过经验驱动而非单纯误差驱动的搜索机制，结合结构化引导，能有效提升符号回归在复杂函数空间中的搜索性能和泛化能力。

---

## Abstract
Symbolic Regression aims to automatically identify compact and interpretable mathematical expressions that model the functional relationship between input and output variables. Most existing search-based symbolic regression methods typically rely on the fitting error to inform the search process. However, in the vast expression space, numerous candidate expressions may exhibit similar error values while differing substantially in structure, leading to ambiguous search directions and hindering convergence to the underlying true function. To address this challenge, we propose a novel framework named EGRL-SR (Experience-driven Goal-conditioned Reinforcement Learning for Symbolic Regression). In contrast to traditional error-driven approaches, EGRL-SR introduces a new perspective: leveraging precise historical trajectories and optimizing the action-value network to proactively guide the search process, thereby achieving a more robust expression search. Specifically, we formulate symbolic regression as a goal-conditioned reinforcement learning problem and incorporate hindsight experience replay, allowing the action-value network to generalize common mapping patterns from diverse input-output pairs. Moreover, we design an all-point satisfaction binary reward function that encourages the action-value network to focus on structural patterns rather than low-error expressions, and concurrently propose a structure-guided heuristic exploration strategy to enhance search diversity and space coverage. Experiments on public benchmarks show that EGRL-SR consistently outperforms state-of-the-art methods in recovery rate and robustness, and can recover more complex expressions under the same search budget. Ablation results validate that the action-value network effectively guides the search, with both the reward function and the exploration strategy playing critical roles.

---

## 论文详细总结（自动生成）

### 论文总结：超越基于误差的优化——基于目标条件强化学习的经验驱动符号回归 (EGRL-SR)

#### 1. 核心问题与整体含义（研究动机和背景）
符号回归（Symbolic Regression, SR）的核心挑战在于其搜索空间随表达式长度呈指数级增长（NP-hard）。
*   **研究动机**：现有的搜索类方法（如遗传算法、强化学习、蒙特卡洛树搜索）主要依赖**拟合误差**作为引导信号。然而，在复杂的函数空间中，大量结构迥异的表达式在特定区间内可能具有极低的相似误差（如图 2 所示），这导致搜索方向模糊，算法容易陷入局部最优，生成结构冗余或外推性能差的公式。
*   **整体含义**：本文提出 **EGRL-SR** 框架，试图将 SR 从传统的“误差驱动”转向“经验驱动”。通过学习历史构造轨迹中的结构模式，而非仅仅追求降低误差，从而更稳健地发现底层真实的数学规律。

#### 2. 论文提出的方法论
EGRL-SR 将符号回归任务重新定义为**目标条件马尔可夫决策过程（GC-MDP）**，核心组件包括：
*   **核心思想**：将输入 $x$ 视为起始状态，目标输出 $y$ 视为目标（Goal）。智能体通过后缀表达式（Postfix）逐步构建函数，学习如何从 $x$ 变换到 $y$ 的通用映射模式。
*   **关键技术细节**：
    *   **事后经验回放 (HER)**：针对 SR 奖励稀疏的问题，HER 将失败的尝试（未达到目标 $y$）重新标记为成功（针对该轨迹实际达到的中间值 $y'$），使动作价值网络能从大量“失败”中提取通用的 $x-y$ 变换经验。
    *   **全点满足二元奖励 (APSR)**：放弃连续的误差奖励，采用二元奖励（只有当所有样本点均满足精度阈值时才给 1，否则为 0）。这迫使网络关注结构的一致性，避免被结构错误但误差较小的表达式误导。
    *   **Double Dueling DQN**：采用离线强化学习算法优化动作价值网络，通过状态价值 $V(s)$ 和优势函数 $A(s, a)$ 的分离，提高对动作价值评估的准确性。
    *   **结构引导启发式探索 (SGHE)**：将搜索空间按结构属性（如算子数量、嵌套深度）划分为多个子空间，并分配独立的价值网络进行并行探索，确保搜索的覆盖面和多样性。

#### 3. 实验设计
*   **数据集/场景**：采用了三个经典的符号回归基准测试集：**Nguyen**、**Livermore** 和 **Keijzer**。
*   **Benchmark 设置**：将所有目标表达式按长度分层（从 $\le 8$ 到 $\ge 31$），以评估算法处理不同复杂度的能力。
*   **对比方法**：涵盖了四大类主流方法：
    1.  **强化学习类**：DSR, DSO。
    2.  **预训练/端到端类**：NeSymReS, E2E。
    3.  **遗传算法类**：GOMEA, PySR。
    4.  **其他**：方程学习器（EQL-Div, DySymNet）、蒙特卡洛树搜索（TPSR）。
*   **评估指标**：采用**精确恢复率（Exact Recovery Rate）**，而非 R²，以确保发现的是真实的数学形式。

#### 4. 资源与算力
*   **搜索预算**：每个任务分配统一的搜索预算，即 **160 万步**（由 8 个探索方向组成，每个方向 20 万步）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或具体的训练/搜索总时长。通常此类基于 DQN 的强化学习方法在单卡 GPU 上即可运行，但 160 万步的搜索量在多任务并行下仍需一定的计算资源。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对不同长度区间的数十个表达式进行了恢复率测试。
    *   每个目标表达式均进行了 **12 次独立重复实验**，以消除随机性影响。
    *   进行了**噪声鲁棒性实验**（噪声水平从 0 到 0.1）。
    *   进行了**消融实验**，分别验证了 HER、APSR 和 SGHE 的贡献。
*   **充分性与客观性**：实验设计较为全面，通过分层长度对比和噪声测试，客观地展示了该方法在复杂任务和现实数据中的优势。对比方法涵盖了当前最先进的工具（如 PySR 和 DSO），具有较高的参考价值。

#### 6. 主要结论与发现
*   **恢复能力强**：EGRL-SR 在中高难度（长度 11-30）的表达式恢复上显著优于 DSR 和 PySR 等方法。
*   **鲁棒性高**：在存在高水平高斯噪声的情况下，EGRL-SR 的恢复率下降最慢，表现出极强的抗噪能力，而遗传算法类方法在噪声下几乎失效。
*   **经验引导有效**：消融实验证明，动作价值网络确实学习到了有效的搜索启发式信息，降低了成功所需的轨迹数（TtS）。
*   **结构探索必要性**：SGHE 策略能有效防止算法陷入特定结构的局部最优，对于发现具有复杂映射但结构简洁的公式至关重要。

#### 7. 优点（亮点）
*   **范式创新**：首次将目标条件强化学习（GCRL）引入符号回归，改变了单纯依赖误差反馈的搜索逻辑。
*   **解决结构歧义**：通过 APSR 和 HER，有效地解决了“同误差不同结构”导致的搜索方向迷失问题。
*   **泛化经验**：通过学习 $x-y$ 的映射模式，使得算法能够利用历史搜索经验来加速当前任务的解决。

#### 8. 不足与局限
*   **常数处理能力**：论文提到目前该方法通过变量和算子的组合间接表示常数。对于包含复杂浮点常数的表达式，这种方法会导致搜索空间急剧膨胀，性能下降。
*   **计算开销**：虽然提高了搜索效率，但维护多个价值网络和进行大规模经验回放可能带来额外的内存和计算开销。
*   **算子集依赖**：实验仍基于预定义的算子集，对于算子集之外的函数形式缺乏发现能力。

（完）
