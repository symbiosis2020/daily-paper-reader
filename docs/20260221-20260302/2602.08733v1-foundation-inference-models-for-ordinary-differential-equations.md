---
title: Foundation Inference Models for Ordinary Differential Equations
title_zh: 常微分方程的基础推理模型
authors: "Maximilian Mauel, Johannes R. Hübers, David Berghaus, Patrick Seifner, Ramses J. Sanchez"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08733v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用符号回归和神经算子从噪声轨迹中推断ODE向量场
tldr: 针对从噪声轨迹中推断常微分方程（ODE）向量场的难题，本文提出了FIM-ODE，一种预训练的基础推理模型。该模型通过在低阶多项式向量场分布上进行预训练，利用神经算子实现了仅需单次前向传播即可从噪声数据中直接预测向量场。FIM-ODE在零样本任务中表现优异，且通过微调能快速适应复杂系统，性能超越了现有的符号回归和神经ODE基准，显著降低了对专家知识的依赖。
selection_source: fresh_fetch
motivation: 传统的ODE推断方法如符号回归和神经ODE通常需要复杂的训练流程或特定的先验知识，难以在缺乏专家指导的情况下高效应用。
method: 提出FIM-ODE模型，通过在低阶多项式向量场先验上预训练神经算子，实现从噪声轨迹数据到向量场的直接摊销推理。
result: 该模型在零样本性能上匹配并常优于ODEFormer，且通过快速微调在稳定性和准确性上超越了现代神经和高斯过程基准。
conclusion: FIM-ODE作为一种基础推理模型，为科学建模中的ODE推断提供了一种高效、通用且易于使用的自动化解决方案。
---

## 摘要
常微分方程（ODEs）是科学建模的核心，但从噪声轨迹中推断其向量场仍然具有挑战性。目前的方法，如符号回归、高斯过程（GP）回归和神经微分方程（Neural ODEs），通常需要复杂的训练流程和大量的机器学习专业知识，或者强烈依赖于特定系统的先验知识。我们提出了 FIM-ODE，这是一种预训练的基础推理模型，通过在单次前向传递中直接从噪声轨迹数据预测向量场，从而实现了低维 ODE 推理的摊销。我们在具有低阶多项式向量场的 ODE 先验分布上对 FIM-ODE 进行预训练，并使用神经算子表示目标场。FIM-ODE 展现了强大的零样本性能，在多种方案下均能达到甚至优于最近的预训练符号基准模型 ODEFormer，尽管其使用了更简单的预训练先验分布。预训练还为微调提供了强大的初始化，实现了快速且稳定的自适应，在无需机器学习专业知识的情况下，其表现优于现代神经和高斯过程基准模型。

## Abstract
Ordinary differential equations (ODEs) are central to scientific modelling, but inferring their vector fields from noisy trajectories remains challenging. Current approaches such as symbolic regression, Gaussian process (GP) regression, and Neural ODEs often require complex training pipelines and substantial machine learning expertise, or they depend strongly on system-specific prior knowledge. We propose FIM-ODE, a pretrained Foundation Inference Model that amortises low-dimensional ODE inference by predicting the vector field directly from noisy trajectory data in a single forward pass. We pretrain FIM-ODE on a prior distribution over ODEs with low-degree polynomial vector fields and represent the target field with neural operators. FIM-ODE achieves strong zero-shot performance, matching and often improving upon ODEFormer, a recent pretrained symbolic baseline, across a range of regimes despite using a simpler pretraining prior distribution. Pretraining also provides a strong initialisation for finetuning, enabling fast and stable adaptation that outperforms modern neural and GP baselines without requiring machine learning expertise.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FIM-ODE** 的基础推理模型（Foundation Inference Model），旨在解决从噪声轨迹数据中高效推断常微分方程（ODE）向量场的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从稀疏、含有噪声的观测轨迹中准确且快速地推断出底层的动力学系统（即 ODE 的向量场）。
*   **研究背景**：传统方法（如符号回归、高斯过程回归、神经 ODE）通常需要针对每个新数据集进行复杂的重新训练或参数搜索，且对超参数和专家知识依赖较强。
*   **整体含义**：论文提出了一种“摊销推理”（Amortized Inference）范式。通过在大规模合成数据上预训练一个基础模型，使其能够通过单次前向传播（Zero-shot）直接预测未知系统的向量场，显著降低了推理成本。

### 2. 方法论
*   **核心思想**：将 ODE 推断视为一个条件生成/回归任务，利用 Transformer 架构学习从观测轨迹到局部向量场映射的通用规律。
*   **关键技术细节**：
    *   **预训练先验**：构建了一个包含 60 万个 1D 到 3D 的 ODE 系统数据集，其向量场由随机系数的低阶多项式（最高 3 阶）组成。
    *   **输入表示**：不直接输入原始坐标，而是构造“转换特征”（Transition Features），包括当前状态 $y_i$、位移 $\Delta y_i$、平方位移 $\Delta y_i^2$ 和时间间隔 $\Delta \tau_i$。
    *   **架构设计**：采用编码器-解码器结构。编码器通过自注意力机制处理无序的转换特征；解码器通过交叉注意力机制，根据查询位置 $x$ 输出该点的向量场估计 $\hat{f}(x)$。
    *   **归一化**：引入可逆实例归一化（RevIN）和时间增量对数中心化，以实现对不同空间和时间尺度的尺度不变性。
    *   **损失函数**：使用带有不确定性权重的 L1 损失（基于拉普拉斯似然），使模型能自动识别并降低数据稀疏区域的权重。

### 3. 实验设计
*   **数据集/场景**：
    *   **ODEBench**：包含 61 个经典的科学建模 ODE 系统（如范德波尔振子、洛伦兹吸引子等），涉及非多项式（三角、指数）函数。
    *   **低数据量 OOD 场景**：针对 Van der Pol (VDP) 和 FitzHugh-Nagumo (FHN) 系统进行预测和插值。
    *   **真实世界数据**：CMU Motion Capture（人体动作捕捉数据），测试模型在复杂生物运动轨迹上的表现。
*   **对比基准（Benchmarks）**：
    *   **预训练基准**：ODEformer（目前最先进的符号回归基础模型）。
    *   **经典/神经基准**：NeuralODE、GP-DNF、GPODE、npODE、LatentSDE 等。

### 4. 资源与算力
*   **模型规模**：总参数量约 1300 万（13M），其中向量场预测器 8M，不确定性估计头 5M。
*   **算力投入**：使用了 **4 张 NVIDIA A40 GPU**（每张 48GB 显存），训练时长约为 **5 天**。
*   **预训练规模**：在 60 万个合成 ODE 系统上进行训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ODEBench 的 61 个系统上进行了多种噪声水平（$\sigma=0, 0.03, 0.05$）和采样率（$\rho=0, 0.5$）的组合测试。
    *   对合成多项式系统进行了 4000 组测试，并包含 100 组针对噪声敏感性的独立试验。
    *   进行了关于数据集规模（10k 到 200k）和离散化程度的消融实验。
*   **充分性评价**：实验覆盖了从合成到真实、从分布内到分布外的多种场景，对比了符号、神经、高斯过程等多种技术路线，实验设计较为客观、公平且充分。

### 6. 主要结论与发现
*   **局部表示的优越性**：相比于 ODEformer 追求全局符号表达式，FIM-ODE 采用的局部神经算子表示在处理分布外（OOD）系统时更具鲁棒性。
*   **零样本能力**：尽管预训练先验仅为简单的多项式，但模型能有效泛化到包含三角函数和有理函数的复杂系统。
*   **微调优势**：预训练权重为下游任务提供了极佳的初始化，通过极少量的微调（Finetuning），FIM-ODE 在人体动作捕捉等复杂任务上超越了所有专门训练的非参数化基准模型。

### 7. 优点
*   **高效性**：推理速度极快，单次前向传播即可完成。
*   **易用性**：无需复杂的超参数调节或机器学习专业背景即可使用。
*   **灵活性**：能够处理不规则采样、多轨迹输入，并能通过微调快速适应极端 OOD 数据。
*   **尺度不变性**：通过专门的归一化设计，解决了 ODE 系统跨度巨大的数值范围问题。

### 8. 不足与局限
*   **维度限制**：目前仅支持最高 **3 维** 的系统，这限制了其在超大规模动力系统中的应用。
*   **先验偏差**：多项式先验可能导致模型在某些保守系统（如无摩擦单摆）中产生向原点收缩的偏差。
*   **数值稳定性**：多项式向量场在远离观测区域时容易发散，导致长程预测可能失效。
*   **架构固定**：状态维度在设计时是固定的，未来需要更灵活的架构（如轴向注意力）来处理变长维度。

（完）
