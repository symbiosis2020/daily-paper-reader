---
title: "Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery"
title_zh: 先验引导的符号回归：迈向方程发现中的科学一致性
authors: "Jing Xiao, Xinhai Chen, Jiaming Peng, Qinglin Wang, Menghan Jia, Zhiquan Lai, Guangping Yu, Dongsheng Li, Tiejun Li, Jie Liu"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13021v2"
tags: ["query:sr"]
score: 10.0
evidence: 先验引导的符号回归以确保科学一致性
tldr: 针对符号回归中方程虽拟合数据但违背科学原理的“伪方程陷阱”，本文提出PG-SR框架。该框架通过预热、演化和精炼三阶段流程，引入先验约束检查器和先验退火约束评估机制（PACE），将领域先验转化为可执行约束，引导搜索过程。理论证明其降低了假设空间复杂度，实验显示其在多领域优于现有方法，且对噪声和数据稀缺具有鲁棒性。
selection_source: fresh_fetch
motivation: 现有的符号回归方法过度依赖经验风险最小化，导致发现的方程往往符合数据观测却违背基本的科学原理。
method: 提出PG-SR框架，利用先验约束检查器将领域知识编码为约束，并通过先验退火约束评估机制（PACE）在演化过程中引导方程发现。
result: 实验表明PG-SR在多个领域均优于最先进的基准方法，并在面对低质量先验、噪声数据和数据稀缺时表现出极强的鲁棒性。
conclusion: 该研究通过引入先验引导机制，有效解决了符号回归中的科学一致性问题，为发现具有物理意义的科学方程提供了理论保障和实践路径。
---

## 摘要
符号回归 (SR) 旨在从观测数据中发现可解释的方程，具有揭示自然现象背后潜在原理的潜力。然而，现有方法往往会陷入“伪方程陷阱”：生成的方程虽然能很好地拟合观测数据，但与基本科学原理不一致。一个关键原因是这些方法由经验风险最小化主导，缺乏确保科学一致性的显式约束。为了弥补这一差距，我们提出了 PG-SR，这是一个基于预热、进化和精炼三阶段流水线构建的先验引导符号回归框架。在整个流水线中，PG-SR 引入了一个先验约束检查器，将领域先验显式编码为可执行的约束程序，并在进化阶段采用先验退火约束评估 (PACE) 机制，逐步引导发现过程转向符合科学一致性的区域。在理论上，我们证明了 PG-SR 降低了假设空间的 Rademacher 复杂度，从而产生了更紧致的泛化界限，并为防止伪方程提供了保证。在实验上，PG-SR 在多个领域均优于最先进的基准方法，在面对不同的先验质量、噪声数据和数据稀缺时保持了鲁棒性。

## Abstract
Symbolic Regression (SR) aims to discover interpretable equations from observational data, with the potential to reveal underlying principles behind natural phenomena. However, existing approaches often fall into the Pseudo-Equation Trap: producing equations that fit observations well but remain inconsistent with fundamental scientific principles. A key reason is that these approaches are dominated by empirical risk minimization, lacking explicit constraints to ensure scientific consistency. To bridge this gap, we propose PG-SR, a prior-guided SR framework built upon a three-stage pipeline consisting of warm-up, evolution, and refinement. Throughout the pipeline, PG-SR introduces a prior constraint checker that explicitly encodes domain priors as executable constraint programs, and employs a Prior Annealing Constrained Evaluation (PACE) mechanism during the evolution stage to progressively steer discovery toward scientifically consistent regions. Theoretically, we prove that PG-SR reduces the Rademacher complexity of the hypothesis space, yielding tighter generalization bounds and establishing a guarantee against pseudo-equations. Experimentally, PG-SR outperforms state-of-the-art baselines across diverse domains, maintaining robustness to varying prior quality, noisy data, and data scarcity.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：PG-SR (Prior-Guided Symbolic Regression)

#### 1. 核心问题与研究动机
*   **核心问题**：论文指出了符号回归（SR）中的**“伪方程陷阱”（Pseudo-Equation Trap）**。即现有方法（基于搜索、Transformer或LLM）过度依赖**经验风险最小化（ERM）**，导致发现的方程在训练集上拟合度极高，但因违背基本科学原理（如物理守恒、生物单峰响应等），在分布外（OOD）数据上表现极差。
*   **研究背景**：在现实科学发现中，观测数据往往伴随噪声且稀缺，这进一步加剧了伪方程的产生。现有的隐式约束（如算子限制、复杂度惩罚）不足以确保科学一致性。

#### 2. 方法论：PG-SR 框架
PG-SR 提出了一个由先验引导的三阶段流水线，核心思想是将人类验证的领域知识转化为可执行的约束程序。
*   **核心组件**：
    *   **先验约束检查器（Prior Constraint Checker）**：利用 LLM 提取领域知识，经人工验证后转化为 Python 脚本，对候选方程进行物理/科学一致性校验。
    *   **先验退火约束评估（PACE）**：一种动态评分机制。在搜索初期允许违反约束以保持多样性，随迭代进行（退火），逐步加大对违规方程的惩罚，引导搜索进入科学一致的子空间。
*   **三阶段流程**：
    1.  **预热（Warm-up）**：基于数据统计分析，利用 LLM 生成初步的方程骨架并进行参数优化。
    2.  **演化（Evolution）**：利用 LLM 作为演化算子，结合 PACE 评分从经验池中采样并改进方程。
    3.  **精炼（Refinement）**：引入残差分析和反射机制，针对拟合不佳的区域进行结构化修正，并总结成功/失败经验存入“见解池”。
*   **理论支撑**：通过统计学习理论证明，引入先验约束降低了假设空间的 **Rademacher 复杂度**，从而获得更紧致的泛化界限。

#### 3. 实验设计
*   **数据集/场景**：涵盖生物（大肠杆菌生长动力学）、材料（铝合金应力-应变实测数据）、化学（反应动力学 CRK）及物理（两种非线性振荡器）四大领域。
*   **Benchmark**：使用 LLM-SRBench 作为基础，确保数据未被 LLM 预训练记忆。
*   **对比方法**：
    *   **搜索类**：PySR, GPLearn, DSR, uDSR, Operon, GP-GOMEA。
    *   **Transformer 类**：E2E, NeSymReS, TPSR, PhyE2E。
    *   **LLM 类**：LLM-SR, LaSR, DrSR。

#### 4. 资源与算力
*   **硬件配置**：Intel Core i9-14900HX CPU，单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **推理成本**：以 Oscillator1 任务为例，单次完整运行约需 **10 小时**。
*   **API 消耗**：使用 `gpt-4o-mini` 作为后端，单次运行约消耗 1785 万 tokens，成本约 **4.70 美元**。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的任务上进行了 ID（分布内）和 OOD（分布外）测试。
*   **消融实验**：针对预热阶段、PACE 机制、精炼阶段分别做了剔除实验，验证了各模块的必要性。
*   **鲁棒性测试**：进行了**先验质量敏感性分析**（无先验、弱先验、错误先验、全先验）、**噪声鲁棒性**（不同高斯噪声水平）及**数据稀缺性**（5% 到 100% 数据量）实验。
*   **评价**：实验设计全面且客观，特别是针对“错误先验”的测试证明了系统不会因误导信息而崩溃，体现了实验的严谨性。

#### 6. 主要结论与发现
*   **性能领先**：PG-SR 在所有数据集的 OOD 泛化指标上均显著优于基准方法，证明了科学一致性对发现真实方程的重要性。
*   **克服陷阱**：显式先验约束能有效过滤掉那些“过度拟合噪声”的伪方程。
*   **数据效率**：在仅有 5% 数据的情况下，依靠先验引导，PG-SR 仍能保持稳定的发现能力。
*   **模型无关性**：使用 Llama-3.3-70B 替代 GPT-4o-mini 仍能取得相似结果，证明框架具有通用性。

#### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了框架，还给出了严谨的泛化误差界限证明。
*   **PACE 机制**：巧妙地解决了“硬约束”可能导致的搜索停滞问题，通过动态退火平衡了探索与利用。
*   **残差反射**：精炼阶段的见解池（Insight Pool）让 LLM 能够学习失败教训，模拟了科学家的反思过程。

#### 8. 不足与局限
*   **人工干预**：虽然使用了 LLM 辅助，但先验约束的最终验证和程序化仍需要**人工介入**，尚未实现完全自动化的“端到端”发现。
*   **计算开销**：相比于传统的遗传算法（如 PySR），基于 LLM 的迭代推理时间较长且存在 API 调用成本。
*   **先验依赖**：虽然对错误先验有一定鲁棒性，但系统的上限仍高度依赖于所提供先验知识的准确性和完备性。

（完）
