---
title: An information-based model selection criterion for data-driven model discovery
title_zh: 一种用于数据驱动模型发现的基于信息的模型选择准则
authors: "Michael C Chung, Alen Zacharia, Juan Guan"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21393v1"
tags: ["query:sr"]
score: 10.0
evidence: 从数据中提取可解释的符号模型
tldr: 针对数据驱动模型发现中难以平衡模型复杂性与准确性的问题，本文提出了一种新型信息准则 SLIC 及其配套的稀疏回归算法。该方法能自动生成候选模型并利用 SLIC 准则自适应地筛选出最优模型，有效避免了人工调参和过拟合。通过在多种非线性微分方程及流体力学、纳米技术实验数据上的验证，证明了 SLIC 在提取可解释物理模型和生成可测试预测方面显著优于传统准则。
selection_source: fresh_fetch
motivation: 现有的数据驱动模型发现算法在平衡拟合度与稀疏性时常需繁琐的人工调参，且容易出现过拟合现象。
method: 提出一种自动生成候选模型的稀疏回归算法，并引入样本长度缩放对数信息准则（SLIC）来识别最优模型。
result: 实验表明 SLIC 在处理非线性微分方程和真实实验数据时，其模型提取准确率显著超过了其他流行的方法选择准则。
conclusion: 该研究为从复杂数据中自动发现具有可解释性和预测能力的科学模型提供了一种稳健且高效的自动化工具。
---

## 摘要
数据驱动的模型发现 (DDMD) 算法是从数据中提取可解释符号模型的强大工具。然而，识别出能够最佳平衡拟合优度与稀疏性的模型通常是一个费力的过程，需要用户进行微调，且容易出现过拟合，其结果可能会因模型初始化和特定训练过程的不同而产生显著差异。在本文中，我们提出了一种稀疏回归算法，该算法能够自动且自适应地生成候选模型，并使用一种新型的样本长度缩放对数信息准则 (SLIC) 从这些候选模型中识别出最佳模型。我们证明了在从几种非线性常微分方程和偏微分方程的数据中提取正确模型方面，SLIC 的表现大大优于其他流行的信息准则。随后，我们展示了 SLIC 从流体动力学和纳米技术的实验数据集中发现可解释模型的能力，这些模型能够产生新的可测试预测。

## Abstract
Data-driven model discovery (DDMD) algorithms are powerful tools for extracting interpretable symbolic models from data. However, identifying the model that best balances goodness-of-fit and sparsity is often a laborious process requiring user fine-tuning, is prone to overfitting, and results may significantly vary depending on model initialization and specific training procedure. Here, we present a sparse regression algorithm that automatically and adaptively generates candidate models, and uses a novel sample-length-scaling logarithmic information criterion (SLIC) to identify the best model from these candidates. We demonstrate that SLIC greatly outperforms other popular information criteria in extracting the correct model from the data of several nonlinear ordinary and partial differential equations. Then, we demonstrate SLIC's ability to discover interpretable models from experimental datasets in fluid dynamics and nanotechnology that generate new testable predictions.

---

## 论文详细总结（自动生成）

以下是对论文《An information-based model selection criterion for data-driven model discovery》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在数据驱动模型发现（DDMD）中，如何自动且稳健地从候选模型中筛选出既能准确拟合数据又具备物理可解释性（即稀疏性）的最优模型。
*   **研究背景**：
    *   传统的符号回归或稀疏回归方法（如 SINDy）高度依赖人工调节稀疏参数（如阈值 $\lambda$）。
    *   现有的模型选择准则（如 AIC, BIC）在处理大规模数据时，其拟合优度项的增长速度远超稀疏惩罚项，导致算法倾向于包含不必要的冗余项，产生过拟合。
    *   缺乏一种能够随数据量自适应缩放、无需人工干预且对噪声鲁棒的通用准则。

### 2. 论文提出的方法论
*   **核心思想**：提出了一种名为 **SLIC**（Sample-Length-scaling logarithmic Information Criterion，样本长度缩放对数信息准则）的新型评分函数，并结合自适应稀疏回归算法实现自动化模型发现。
*   **关键技术细节**：
    *   **SLIC 公式**：$SLIC(\hat{\epsilon}, k) = n \log(\hat{\epsilon} k)$。其中 $n$ 是观测点数量，$\hat{\epsilon}$ 是均方误差（MSE），$k$ 是模型复杂度（非零参数数量）。
    *   **推导逻辑**：基于缩放对称性原理，要求准则在处理不同数据量时保持模型不变性。SLIC 将惩罚项设计为随 $n$ 线性缩放，并对 $k$ 取对数，从而在精度和稀疏性之间实现“公平”权衡。
    *   **方差关联**：论文证明了最小化 SLIC 等价于最小化模型方差（Model Variance），这从统计学上解释了其抗过拟合的能力。
*   **算法流程**：
    1.  **初始化**：通过伪逆获得初始模型估计。
    2.  **自动阈值生成**：直接利用当前系数的绝对值作为候选阈值数组。
    3.  **迭代剪枝**：遍历阈值，对模型进行剪枝并重新计算系数。
    4.  **SLIC 评分**：使用 SLIC 对所有候选模型评分，保留得分最低（最优）的模型。
    5.  **弱形式转化**：采用 Galerkin 投影（弱形式）处理微分方程，避免对噪声数据直接求导。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成 ODE（常微分方程）**：包括 Lorenz 系统、Rössler 吸引子、Lotka-Volterra 模型、Brusselator、Van der Pol 振子和非线性摆。
    *   **合成 PDE（偏微分方程）**：包括 Burger's 方程、KdV 方程、Kuramoto-Sivashinsky (KS) 方程、非线性薛定谔方程 (NLS) 和 Sine-Gordon 方程。
    *   **真实实验数据**：矩形箱体内的流体晃动（Sloshing fluid）中心动力学数据；用于癌症免疫疗法的 RNA-脂质体纳米颗粒团聚数据。
*   **Benchmark 与对比方法**：
    *   对比了 6 种主流信息准则：**AIC** (Akaike), **AICc** (修正 AIC), **HQIC** (Hannan-Quinn), **BIC** (Bayesian), **KIC** (Kashyap), 以及 **BC** (Bridge Criterion)。
    *   评估指标包括：模型项识别准确率（Accuracy）、假阳性率（FPR）和缩放平均绝对误差（SMAE）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、显存或 CPU 核心数）。
*   **软件环境**：实验主要基于 **Julia 语言**生态系统实现，使用了 `OrdinaryDiffEq.jl` 进行数值模拟，`DiffEqFlux.jl` 进行参数估计，以及 `Optimization.jl` 进行非线性优化。由于稀疏回归主要涉及矩阵运算和简单的迭代，其计算开销通常远低于深度学习模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每种 ODE 和 PDE 系统，在不同的噪声水平（最高达 40%）下分别进行了 **25 次独立随机重复实验**，以获取统计显著性。
    *   进行了关于采样频率、轨迹长度、下采样比例以及方程参数变化的鲁棒性测试（消融实验）。
*   **充分性评价**：实验设计非常充分且客观。它不仅在理想的合成数据上验证了算法，还挑战了高噪声环境，并跨越了从简单 ODE 到复杂多维 PDE 的多种物理场景。对比基准涵盖了过去几十年中最具代表性的统计准则，对比具有说服力。

### 6. 论文的主要结论与发现
*   **卓越的鲁棒性**：SLIC 在所有测试的 ODE 和 PDE 系统中，其准确率和假阳性率表现均显著优于 AIC、BIC 等传统准则，尤其在高噪声环境下，SLIC 几乎不引入冗余项。
*   **解决过拟合悖论**：传统准则随数据量增加反而更容易过拟合，而 SLIC 保持了模型选择的一致性。
*   **强大的预测能力**：
    *   在流体晃动实验中，SLIC 仅利用无受迫振动的数据就准确预测了受迫振动下的滞后现象和不稳定区间。
    *   在纳米技术实验中，SLIC 成功提取了团聚核函数（Kernel），并准确预测了稀释条件下的颗粒生长速率。

### 7. 优点
*   **无需调参**：通过 SLIC 评分实现了全自动的阈值选择，消除了 DDMD 中最耗时的人工微调环节。
*   **理论优雅**：SLIC 的推导基于对称性原理，且与偏置-方差权衡（Bias-Variance Tradeoff）有直接的数学联系。
*   **泛化性强**：不仅适用于模拟数据，在跨学科的真实实验数据（流体、生物纳米材料）上也表现出极强的物理发现能力。

### 8. 不足与局限
*   **框架限制**：该方法目前主要建立在稀疏回归（Sparse Regression）框架下，对于基于神经网络的黑盒模型发现或纯符号回归（如遗传算法）的适用性尚待进一步验证。
*   **噪声假设**：推导过程中假设噪声为各向同性的高斯噪声，对于非高斯分布或具有强相关性的系统噪声，其表现可能受限。
*   **库依赖性**：模型发现的质量仍取决于预定义的非线性函数库（Library）是否完备，如果真实项不在库中，SLIC 只能选出库内相对最优的模型。

（完）
