---
title: Physics-informed Gaussian Process Regression in Solving Eigenvalue Problem of Linear Operators
title_zh: 物理信息高斯过程回归在求解线性算子特征值问题中的应用
authors: "Tianming Bai, Jiannan Yang"
date: 2026-01-10
pdf: "https://arxiv.org/pdf/2601.06462v1"
tags: ["query:sr"]
score: 6.0
evidence: 用于求解线性算子的物理信息回归
tldr: 针对物理信息高斯过程回归（PI-GPR）在求解特征值问题时因源项为零导致预测均值平凡和边际似然退化的问题，本文提出了一种基于传递函数型指标的新方法。通过构建后验协方差作为特征值的指示器，证明了只有当参数对应算子特征值时后验才非平凡，从而有效提取特征值与特征函数。该方法在多种线性和非线性特征值问题中均表现出良好的有效性。
selection_source: fresh_fetch
motivation: 传统的PI-GPR在处理特征值问题时，由于方程右侧为零，会导致预测结果趋于平凡解且似然函数失效。
method: 借鉴系统辨识思想，利用PI-GP后验构建传递函数型指标，通过后验协方差的非平凡性来识别特征值。
result: 数值实验证明，该方法能准确捕捉线性及非线性算子的特征值，且后验样本均落在对应的特征空间内。
conclusion: 本研究为利用高斯过程解决算子特征值问题提供了一种鲁棒且理论完备的新框架。
---

## 摘要
将物理信息高斯过程回归应用于特征值问题 $(\mathcal{L}-λ)u = 0$ 面临着一个根本性的挑战，即零源项会导致平凡的预测均值和退化的边际似然。受系统辨识的启发，我们利用物理信息高斯过程后验为未知的特征值/特征函数构建了一个传递函数型指标。我们证明，仅当 $λ$ 对应于偏微分算子 $\mathcal{L}$ 的特征值时，后验协方差才是非平凡的，这反映了非平凡特征空间的存在，且来自后验的任何样本都位于该线性算子的特征空间中。我们通过涉及线性和非线性特征值问题的多个数值算例展示了所提方法的有效性。

## 速览
**TLDR**：本文针对物理信息高斯过程回归（PIGP）在求解线性算子特征值问题时面临的零源项导致预测均值平凡及边际似然退化的挑战，提出了一种基于系统辨识思想的新方法。通过构建传递函数型指标，利用后验协方差的非平凡特性来识别特征值，并证明了后验样本均处于算子的特征空间内，为解决线性和非线性特征值问题提供了有效的概率建模框架。 \
**Motivation**：传统的物理信息高斯过程在处理特征值问题时，由于方程右侧为零，会导致模型输出平凡解且无法有效优化参数。 \
**Method**：借鉴系统辨识理论，利用PIGP后验协方差构建传递函数型指标，通过识别协方差非平凡的λ值来确定特征值。 \
**Result**：数值实验证明，该方法能准确捕捉线性和非线性算子的特征值，且生成的后验样本严格落在对应的特征空间内。 \
**Conclusion**：该研究成功扩展了高斯过程在物理约束下的应用范围，为特征值问题的数值求解提供了一种稳健的概率推断新途径。

---

## Abstract
Applying Physics-Informed Gaussian Process Regression to the eigenvalue problem $(\mathcal{L}-λ)u = 0$ poses a fundamental challenge, where the null source term results in a trivial predictive mean and a degenerate marginal likelihood. Drawing inspiration from system identification, we construct a transfer function-type indicator for the unknown eigenvalue/eigenfunction using the physics-informed Gaussian Process posterior. We demonstrate that the posterior covariance is only non-trivial when $λ$ corresponds to an eigenvalue of the partial differential operator $\mathcal{L}$, reflecting the existence of a non-trivial eigenspace, and any sample from the posterior lies in the eigenspace of the linear operator. We demonstrate the effectiveness of the proposed approach through several numerical examples with both linear and non-linear eigenvalue problems.